# ============================================================
# 火瞳（HotpotEye）— Python 依赖
# 版本: v5.3i | 更新: 2026-07-29
# ============================================================

# 核心框架
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pyjwt>=2.8.0
httpx>=0.27.0

# 数据处理
numpy>=1.24.0
pandas>=2.0.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0

# 数据库
psycopg2-binary>=2.9.0
aiosqlite>=0.19.0

# AI/ML 推理
onnxruntime>=1.16.0
torch>=2.0.0              # Jetson GPU推理(边缘端)
ultralytics>=8.0.0        # YOLO检测(边缘端, <8.3兼容Jetson)

# IoT / 消息队列
paho-mqtt>=2.0.0

# 配置 / 工具
pyyaml>=6.0
aiofiles>=23.0.0
python-multipart>=0.0.6

# 测试
pytest>=7.0.0
