# ============================================================
# 火瞳（HotpotEye）— 测试环境依赖（无硬件要求）
# 用于 CI/CD 和本地开发测试
# 版本: v0.4.0 | 2026-08-03
# ============================================================

# === 核心框架 ===
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
pydantic>=2.0.0,<3.0.0
PyJWT>=2.8.0,<3.0.0
httpx>=0.27.0,<1.0.0
httpx2>=2.0.0,<3.0.0          # starlette.testclient 需要

# === 数据处理 ===
numpy>=1.24.0,<3.0.0
pandas>=2.0.0,<3.0.0
Pillow>=10.0.0,<11.0.0

# === 数据库 ===
psycopg2-binary>=2.9.0,<3.0.0
aiosqlite>=0.19.0,<1.0.0

# === 配置/工具 ===
pyyaml>=6.0.0,<7.0.0
aiofiles>=23.0.0,<25.0.0
python-multipart>=0.0.6,<0.1.0

# === IoT ===
paho-mqtt>=2.0.0,<3.0.0

# === 测试 ===
pytest>=7.0.0,<9.0.0
pytest-cov>=4.0.0,<8.0.0
pytest-asyncio>=0.23.0,<1.0.0
requests>=2.31.0,<3.0.0

# === 代码质量 ===
ruff>=0.4.0,<1.0.0              # linter + formatter (替代flake8+black)
mypy>=1.8.0,<2.0.0              # type checker (可选)

# === 注意: 以下依赖需要硬件/GPU，不在测试环境安装 ===
# torch>=2.0.0                    # Jetson GPU推理
# ultralytics>=8.0.0              # YOLO检测
# onnxruntime>=1.16.0            # ONNX推理
# opencv-python-headless>=4.8.0   # 图像处理
