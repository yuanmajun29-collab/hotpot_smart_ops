## UI 规范 — 火瞳暖色牛油火锅暗色体系

> AI生成任何UI前必须先读本章。所有规则以 design-tokens.json 为准。

### Design Token 文件
`hotpot_platform/design-tokens.json` — 颜色/间距/圆角/字体/组件的唯一真实源。
AI 在生成 UI 前必须读取这份文件。人和 AI 共用同一套标准。

### 样式引用
所有页面 <head> 必须引用：
```html
<link rel="stylesheet" href="/assets/theme.css">
```
theme.css 的 CSS 变量从 design-tokens.json 自动生成，禁止手动修改。

### 组件使用规则

| 场景 | 必须用 | 禁止 |
|------|--------|------|
| 按钮 | `.btn` + `.btn-ghost`/`.btn-sm`/`.btn-warn`/`.btn-block` | 裸 `<button>` 无 class |
| 卡片 | `.card` | 自己拼 div 模拟卡片 |
| 表单组 | `.form-group` > label + input | 裸 input 无 label |
| 网格 | `.grid` / `.grid-2` | 自己写 display:grid |
| 导航 | `.sidebar` > `.nav-item` | 自己写侧边栏样式 |
| 顶栏 | `.topbar` > `.topbar-left` + `.topbar-right` | 自己拼 header |
| 抽屉 | `.drawer-overlay` > `.drawer` | 自己写 fixed panel |
| 表格状态 | `.table-cell.empty`/`.dining`/`.need_clean`/`.checkout` | 自己写表格单元格颜色 |
| 标签 | `.tag.pass`/`.fail`/`.pending` | 自己写 inline badge |
| 品质等级 | `.grade.a`/`.b`/`.c`/`.d` | 自己写品质标记 |
| 事件卡片 | `.event.critical`/`.warn`/`.info` | 自己写 border-left 颜色 |
| 统计数字 | `.stat.ok`/`.warn`/`.critical` | 自己写数字颜色 |
| 状态点 | `.status-dot.online`/`.offline` | 自己写 8px 圆点 |

### 颜色使用规则
- 主操作 → `var(--brand)` 或 `.btn`
- 危险操作 → `var(--accent)` 或 `.btn-warn`
- 成功状态 → `var(--ok)` / `.stat.ok`
- 警告状态 → `var(--warn)` / `.stat.warn`
- 链接/信息 → `var(--info)`
- 正文 → `var(--text)`
- 辅助文字 → `var(--muted)`
- 最弱文字 → `var(--faint)`
- 餐桌·用餐中 → `var(--dining)`

### 间距规则
- 卡片间距 → `16px` (spacing.lg)
- 页面 padding → `24px` (spacing.xl)
- 表单组间距 → `16px`
- 导航项 padding → `10px 16px`
- 表单项间距 → `20px`

### 圆角规则
- 卡片 → `10px`
- 按钮/输入框 → `6px`
- Chip/标签 → `4px-20px`
- 抽屉/弹窗 → `12px`

### 禁止事项
1. 禁止硬编码颜色值（#xxx, rgba()）— 必须用 var(--xxx) 或从 token 引用
2. 禁止硬编码像素尺寸 — 必须对照 spacing token
3. 禁止内联样式（style="..."）— 必须用 class
4. 禁止裸 `<button>` 标签 — 必须加 .btn 类
5. 禁止引入第三方 CSS 框架（Bootstrap/Tailwind）— 只用 theme.css
6. 新增颜色/间距必须先更新 design-tokens.json，再跑 token→CSS 生成脚本

### 移动端 H5 规范
- 最大宽度 `480px`，居中
- 用 `.mobile-app` > `.mobile-header` + `.mobile-main` + `.mobile-tabbar` 结构
- Tabbar 固定底部，padding-bottom 用 `env(safe-area-inset-bottom)`
- 顶部用 `env(safe-area-inset-top)`
- 字体不小于 `0.65rem`

### PDA 手持设备规范
- 最大宽度 `480px`，居中
- 用 `.pda-body` > `.pda-header` + `.pda-content` + `.pda-footer` 结构
- 底部固定，留 `80px` 安全区
- 步骤条用 `.pda-stepper` > `.pda-step`

### 数据展示规范
- 数字用 `font-variant-numeric: tabular-nums` 等宽数字
- 大数字 `.stat` (2rem)
- 空状态用 `.empty-state` 结构（图标+标题+描述+操作按钮）
- 加载中必须显示 `.loading` 动画或骨架屏
