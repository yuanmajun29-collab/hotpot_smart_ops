# 火瞳 · 详细架构设计 v1.6

> **版本**：v1.6 | **日期**：2026-08-06 | **状态**：📋 开发参考（权威设计收敛中·第四轮评审修正）
> **上游输入**：[`总体架构 v2.1`](./火瞳_系统架构总体设计_v2.0.md) + [`PRD v5.3n`](./火瞳_融合PRD_v5.3_主线基线.md)
> **下游传递**：→ 开发实施（D1 冻品供应链 / D2 岗位 AI 助理 / D3 集成测试）
> **代码基线**：~43,569 行 / 375 py 文件 / 68 测试文件（2026-08-06 实测）
> **v1.6 变更**：①§0文档关系表同步至v2.1/v5.3n ②N19/N21/N22功能定义再次确认修正(跨店调拨/库位管理/AI推荐) ③映射表标题标注PRD为唯一源 ④附录树版本同步 ⑤新增E.6 API端点迁移表(19项旧→新映射)
> **v1.5 变更**：①E.1端口表改为四列差距表(目标/现状/验证/期限) ②E.3端点状态添加免责声明 ③消除"生产就绪"表述
> **v1.4 变更**：①N19/N21/N22功能定义对齐PRD ②上游引用v5.3m
> **v1.3 变更**：+附录E 接口收敛状态矩阵

---

## 目录

- [§0 文档定位与使用指南](#0-文档定位与使用指南)
- [§1 核心模块接口定义](#1-核心模块接口定义)
  - [1.1 边缘 AI 引擎层](#11-边缘-ai-引擎层)
  - [1.2 Event Hub 平台层](#12-event-hub-平台层)
  - [1.3 数据引擎层](#13-数据引擎层)
  - [1.4 集成桥接层](#14-集成桥接层)
  - [1.5 告警网关层](#15-告警网关层)
  - [1.6 SOP 合规引擎层](#16-sop-合规引擎层) ★新增
  - [1.7 知识库检索层](#17-知识库检索层) ★新增
  - [1.8 仓库 IoT 管理层](#18-仓库-iot-管理层) ★新增
  - [1.9 会员营销层](#19-会员营销层) ★新增
- [§2 数据库 Schema 设计](#2-数据库-schema-设计)
  - [2.1 ER 关系总览](#21-er-关系总览)
  - [2.2 核心表 DDL](#22-核心表-ddl)
  - [2.3 索引策略](#23-索引策略)
  - [2.4 数据生命周期](#24-数据生命周期)
- [§3 RESTful API 规范](#3-restful-api-规范)
  - [3.1 API 设计原则](#31-api-设计原则)
  - [3.2 认证与授权](#32-认证与授权)
  - [3.3 核心 API 端点清单](#33-core-api-endpoints)
  - [3.4 请求/响应格式](#34-请求响应格式)
  - [3.5 错误码体系](#35-错误码体系)
- [§4 事件驱动协议](#4-事件驱动协议)
  - [4.1 事件格式规范](#41-事件格式规范)
  - [4.2 事件类型分类](#42-事件类型分类)
  - [4.3 事件路由规则](#43-事件路由规则)
  - [4.4 边缘→平台上报协议](#44-边缘平台上报协议)
  - [4.5 离线队列与重试](#45-离线队列与重试)
- [§5 边缘-平台通信协议](#5-边缘-平台通信协议)
  - [5.1 心跳协议](#51-心跳协议)
  - [5.2 配置下发协议](#52-配置下发协议)
  - [5.3 OTA 升级协议](#53-ota-升级协议)
- [§6 部署配置清单](#6-部署配置清单)
  - [6.1 环境变量矩阵](#61-环境变量矩阵)
  - [6.2 Docker Compose 模板](#62-docker-compose-模板)
  - [6.3 密钥管理策略](#63-密钥管理策略)
- [§7 监控告警规则](#7-监控告警规则)
  - [7.1 核心指标定义](#71-核心指标定义)
  - [7.2 告警阈值与分级](#72-告警阈值与分级)
  - [7.3 健康检查端点](#73-健康检查端点)
- [§8 接口一致性矩阵](#8-接口一致性矩阵)
- [附录](#附录)

---

## §0 文档定位与使用指南

### 与其他文档的关系

| 文档 | 层级 | 覆盖范围 | 本文档补充 |
|------|:----:|----------|:-----------|
| **系统架构总体设计 v2.1** | L1 | 四层架构+4+1视图 | **L2 接口级细节** |
| **数据引擎技术设计 v1.0** | L1 | N01-N06 算法设计 | **API 端点+Schema DDL** |
| **PRD v5.3n** (唯一权威) | L1 | 129 项功能需求 | **技术实现映射** |
| **本文档 (v1.6)** | **L2** | **接口/数据/API/协议/部署** | — |

### 使用场景

- **开发人员**：实现新功能时查阅接口签名、API 端点、数据模型
- **DevOps**：部署时查阅环境变量、Docker 配置、监控指标
- **测试人员**：编写集成测试时查阅 API 规范、错误码
- **前端/移动端**：对接后端 API 时查阅请求/响应格式

---

## §1 核心模块接口定义

### 1.1 边缘 AI 引擎层

#### 1.1.1 Vision Worker（前厅视觉推理）

```
模块路径: edge/front_hall/inference/vision_worker.py
进程模型: 独立进程 (由 edge agent server :9100 管理)
依赖: YOLOv8 (目标检测) + CLIP (图像理解) + OpenCV
```

**核心函数**: `process_camera(store_id, camera, backend, hub, uat_root, out_dir) -> Dict`

| 参数 | 类型 | 必填 | 说明 |
|------|------|:----:|------|
| `store_id` | str | ✅ | 门店标识，如 `"store_yuhuan"` |
| `camera` | Dict | ✅ | 摄像头配置（来自 `config.json`） |
| `backend` | str | ✅ | 推理后端：`"plan_a"` / `"plan_b"` |
| `hub` | EdgeHubClient | ✅ | 平台通信客户端 |
| `uat_root` | Path | ✅ | UAT 配置根目录 |
| `out_dir` | Path | Optional | 结果输出目录 |

**返回值结构**:
```python
{
    "store_id": "store_yuhuan",
    "camera_id": "cam_front_01",
    "zone": "front",                    # front / kitchen / warehouse
    "timestamp": "2026-07-29T13:00:00Z",
    "frame_path": "/tmp/ipc_frames/front_20260729_130000.jpg",
    "inference_ms": 38.5,               # 推理耗时(ms)
    "status": "ok",                     # ok / error / skipped
    "detections": [...],                # YOLO 检测结果列表
    "analysis": {                       # 策略分析结果
        "table_states": [...],
        "alerts": [...],
        "recommendation": "..."
    },
    "_diagnostics": {
        "yolo_ms": 25.2,
        "clip_ms": 10.1,
        "total_ms": 38.5,
        "frame_size": [1920, 1080]
    }
}
```

**异常处理**:

| 异常 | 触发条件 | 处理方式 |
|------|----------|----------|
| `FileNotFoundError` | 帧文件不存在 | 记录日志，跳过本轮，继续下一帧 |
| `OSError` | IPC 共享内存不可用 | 重试 3 次，间隔 1s，失败后降级到文件模式 |
| `RuntimeError` | 模型加载失败 | 发送告警事件到 Hub，停止该摄像头推理 |

#### 1.1.2 Pipeline 统一推理入口

```
模块路径: edge/front_hall/inference/pipeline.py
设计模式: Strategy Pattern (引擎注册表 + 策略分发)
```

**核心函数**: `analyze_table(image, table_id, image_path, strategy) -> Dict`

| 参数 | 类型 | 必填 | 说明 |
|------|------|:----:|------|
| `image` | np.ndarray | ✅ | BGR 格式图像 (H, W, 3) |
| `table_id` | str | ✅ | 桌号标识，如 `"T01"` |
| `image_path` | str | Optional | 图像文件路径（Plan A 需要） |
| `strategy` | str | ✅ | 策略名：`"plan_b"`(默认) / `"plan_a"` / 自定义 |

**返回值结构**:
```python
{
    "status": "ok",
    "table_id": "T01",
    "zone": "front",
    "timestamp": "...",
    "table_state": {                    # 当前桌态
        "state": "dining",              # empty / dining / need_clean / checkout
        "confidence": 0.92,
        "guests_detected": 4,
        "dishes_on_table": 8,
        "leftover_rate": 0.15           # 剩余率估算
    },
    "alerts": [
        {
            "type": "table_need_clean",
            "level": "info",
            "confidence": 0.85,
            "message": "T01 桌面需要清理"
        }
    ],
    "recommendation": "建议安排服务员清理 T01",
    "_diagnostics": {
        "yolo_ms": 25.2,
        "strategy_ms": 8.3,
        "total_ms": 38.5,
        "ndetections": 12
    }
}
```

#### 1.1.3 引擎注册表

```
模块路径: edge/front_hall/inference/engines/__init__.py
可用引擎: yolo (YOLOv8), clip (CLIP 子进程客户端)
```

| 引擎名 | 类 | 功能 | 内存占用 | 启动方式 |
|--------|-----|------|:--------:|----------|
| `yolo` | YOLODetector | 目标检测（菜品/餐具/人） | ~800MB (GPU) | 懒加载 |
| `clip` | ClipClient | 图像语义理解（CLIP 子进程） | ~400MB (RAM) | 懒加载，子进程隔离 |

**引擎接口契约**:
```python
class BaseEngine(ABC):
    @abstractmethod
    def detect(self, image: np.ndarray, zone: str = "front") -> Dict:
        """执行推理，返回检测结果"""
        ...

    @abstractmethod
    def close(self) -> None:
        """释放资源"""
        ...
```

#### 1.1.4 IoT Bridge（MQTT 传感器桥接）

```
模块路径: edge/front_hall/iot/mqtt_bridge.py
协议: MQTT 3.1.1 (paho-mqtt)
Broker: 本地 Mosquitto 或云端 MQTT
```

**核心函数**: `parse_payload(topic_cfg, payload) -> Optional[Dict]`

| 参数 | 类型 | 说明 |
|------|------|------|
| `topic_cfg` | Dict | 传感器主题配置（来自 `mqtt_topics.json`） |
| `payload` | bytes | MQTT 原始载荷 |

**返回值**:
```python
{
    "sensor_id": "temp_freezer_01",
    "type": "temperature",
    "unit": "°C",
    "value": -15.5,
    "timestamp": "2026-07-29T13:05:00Z",
    "level": "info",                  # info / warn / critical
    "thresholds": {"warn": -12, "critical": -8},
    "store_id": "store_yuhuan",
    "zone": "warehouse"
}
```

**支持的传感器类型**:

| 类型 | 单位 | 典型阈值 (warn/critical) | 上报频率 |
|------|------|-------------------------|:--------:|
| temperature | °C | 冷链: -12/-8; 后厨: 28/35 | 30s |
| humidity | %RH | 仓库: 70/85 | 60s |
| weight | kg | 短重: -3%/-5% | 实时（称重触发） |
| door_state | bool | 开门超时: 300s/600s | 状态变化时 |
| rfid_scan | string | 缺失: 未扫描超时 | 扫描时 |

---

### 1.2 Event Hub 平台层

#### 1.2.1 MultiTenantHub（多租户事件中枢）

```
模块路径: hotpot_platform/cloud/event_hub/hub_core.py
设计模式: Repository + Observer (事件订阅发布)
存储: SQLite (dev) / PostgreSQL (prod)
```

**类**: `MultiTenantHub`

| 方法 | 签名 | 说明 |
|------|------|------|
| `get_store(store_id)` | `-> EventStore` | 获取门店事件存储实例 |
| `seed_from_directory(dir)` | `-> None` | 从目录初始化门店数据 |
| `anomaly_stores()` | `-> List[Dict]` | 返回异常门店列表 |

**类**: `EventStore` (单店租户)

| 方法 | 签名 | 说明 |
|------|------|------|
| `add_event(event)` | `-> Dict` | 新增事件，自动生成 event_id 和 timestamp |
| `get_events(level, limit)` | `-> List[Dict]` | 查询事件（按级别过滤） |
| `get_summary()` | `-> Dict` | 门店运营摘要 |
| `update_table(table_id, state)` | `-> None` | 更新桌态 |
| `update_snapshot(kind, payload)` | `-> None` | 更新快照数据 |

**事件订阅机制**:
```python
# 注册订阅者（数据引擎等消费者）
def subscribe_to_events(callback: Callable[[Dict], None]) -> None: ...
def unsubscribe_from_events(callback: Callable[[Dict], None]) -> None: ...

# 回调签名
def on_event(event: Dict[str, Any]) -> None:
    """
    event 结构:
    {
        "event_id": "uuid-v4",
        "store_id": "store_yuhuan",
        "level": "critical/warn/info",
        "source": "vision/iot/pos/system",
        "payload": {...},
        "created_at": "ISO-8601"
    }
    """
```

#### 1.2.2 FastAPI 应用主入口

```
模块路径: hotpot_platform/cloud/event_hub/app.py
框架: FastAPI + Uvicorn
端口: 8088 (可配置)
中间件: CORS + Auth (JWT + API Key)
```

**应用配置**:

| 配置项 | 环境变量 | 默认值 | 说明 |
|--------|----------|:------:|------|
| 部署环境 | `HOTPOT_ENV` | `dev` | dev/staging/uat/production |
| 认证模式 | `HOTPOT_AUTH_MODE` | `demo` | demo/strict |
| 数据库 URL | `HOTPOT_DATABASE_URL` | 空(SQLite) | PostgreSQL 连接串 |
| JWT 密钥 | `HOTPOT_JWT_SECRET` | `hotpot-dev-secret-change-in-prod` | **生产必须修改** |
| JWT 过期时间 | `HOTPOT_JWT_HOURS` | `24` | Token 有效期(小时) |
| CORS 来源 | `HOTPOT_CORS_ORIGINS` | `*`(dev) | 逗号分隔的允许来源 |

**生命周期钩子**:
```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    # startup:
    #   1. 初始化数据库 (SQLite/PostgreSQL)
    #   2. 加载 MultiTenantHub
    #   3. 初始化 AlertGateway
    #   4. 启动 DailyReportScheduler (定时任务)
    # shutdown:
    #   1. 关闭数据库连接
    #   2. 停止调度器
    yield
```

#### 1.2.3 路由注册表

```
模块路径: hotpot_platform/cloud/event_hub/routers/
共 26 个路由模块, 120+ 个端点
```

| 路由模块 | 前缀 | 端点数 | 核心功能 |
|----------|------|:------:|----------|
| `auth_routes.py` | `/auth` | 2 | 登录/Token 刷新 |
| `devices.py` | `/api/v1/devices` | 5 | 设备注册/心跳/配置 |
| `ingest.py` | `/ingest`, `/events` | 8 | 事件摄入(legacy) |
| `alerts.py` | `/alerts/*` | 6 | 告警确认/推送/审计 |
| `kitchen.py` | `/api/v1/kitchen/waste` | 5 | 后厨废料统计/告警 |
| `receiving.py` | `/api/v1/receiving` | 7 | 收货质检/提交/签名 |
| `inventory.py` | `/inventory/{store_id}` | 5 | 库存查询/调整/变动 |
| `orders.py` | `/orders/*` | 4 | 订货建议/审批 |
| `forecast.py` | `/forecast/{store_id}` | 3 | 销量预测/生成 |
| `cost.py` | `/api/v1/cost` | 4 | 成本控制/损耗预算 |
| `sop.py` | `/api/v1/sop` | 6 | SOP 合规/分配 |
| `tasks.py` | `/v1/tasks` | 6 | 任务管理(CRUD+动作) |
| `daily_report.py` | `/api/v1/daily/report` | 2 | 日报生成/查询 |
| `analytics.py` | `/analytics` | 3 | 分析聚合 |
| `vlm.py` | `/api/v1/vlm` | 3 | VLM 图片审核/废料估算 |
| `iot.py` | `/v1/iot` | 3 | IoT 读数批量写入 |
| `staff_behavior.py` | `/api/v1/staff-behavior` | 4 | 员工行为识别 |
| `turnover.py` | `/api/v1/turnover` | 1 | 翻台率建议 |
| `reports.py` | `/v1/reports` | 2 | 报表生成 |
| `org.py` | `/v1/org` | 2 | 组织架构 |
| `admin.py` | `/v1/admin/*` | 8 | 管理后台 |
| `system.py` | `/v1/system` | 2 | 系统通知/状态 |
| `feedback.py` | `/feedback` | 1 | 用户反馈 |
| `images.py` | `/v1/images` | 2 | 图片上传/查询 |
| `waste_trend.py` | `/api/v1/waste` | 3 | 废料趋势 |
| `loss/*.py` | `/loss/{store_id}` | 5 | 损耗分析 |

---

### 1.3 数据引擎层

#### 1.3.1 SalesPredictor（销量预测 N01）

```
模块路径: hotpot_platform/cloud/data_engine/sales_predictor.py
降级链: L1(RuleBaseline) → L2(SARIMA) → L3(LightGBM) → L4(LLM增强)
```

**类**: `SalesPredictor`

```python
class SalesPredictor:
    def __init__(
        self,
        data_loader: DataLoader,       # (store_id, sku, start, end) -> List[SalesRecord]
        sku_lister: SkuLister,          # (store_id) -> List[sku]
        model_dir: Path = ...,          # 模型文件目录
    ) -> None: ...

    def predict(
        self,
        store_id: str,
        sku: str,
        forecast_date: date,
        horizon_days: int = 1,
    ) -> SalesForecast:
        """
        返回 SalesForecast:
        {
            store_id, sku, forecast_date,
            predicted_qty: float,      # 预测销量(份/kg)
            confidence: float,         # 0~1 置信度
            lower_bound: float,        # 置信区间下界
            upper_bound: float,        # 置信区间上界
            model_version: str,        # "L1-rule" / "L2-sarima" / "L3-lgb" / "L4-llm"
            features_used: Dict        # 使用的特征
        }
        """
        ...
```

**四级降级链详情**:

| 级别 | 算法 | 数据需求 | 准确度(MAPE) | 可用性 |
|:----:|------|----------|:------------:|:------:|
| L1 | 移动平均 + 同周环比 | 7 天 | ~18% | ✅ 永远可用 |
| L2 | SARIMA 季节性时序 | 60 天 | ~14% | ⚠️ 需 statsmodels |
| L3 | LightGBM 多因子回归 | 90 天 | ~11% | ⚠️ 需 60+天数据 |
| L4 | LLM 自然语言增强 | 90 天 + LLM | ~10% | ⚠️ 需 LLM API |

#### 1.3.2 OrderAdvisor（订货顾问 N02）

```
模块路径: hotpot_platform/cloud/data_engine/order_advisor.py
三模型混合: EOQ / 报童模型 / 动态安全库存
```

**决策流**:
```
预测需求(N01) → 计算可用库存(N03) → gap = 需求 - 库存 - 在途
→ 按品类保质期选模型:
    · 保质期 > 30天  → EOQ: Q* = √(2DS/H)
    · 保质期 ≤ 3天   → 报童模型: Q* = μ + Z·σ
    · 中间品 4~30天  → 动态安全库存: ROP = d̄·L + Z·σ_L·√L
→ 约束调整(最小起订量/包装单位/供应商MOQ)
→ 生成 OrderSuggestion + 紧急度判定
```

**返回值**: `OrderSuggestion` (见 models.py)

#### 1.3.3 LossAnalyzer（损耗分析 N04）

```
模块路径: hotpot_platform/cloud/data_engine/loss_analyzer.py
继承: CostControlAnalyzer
损耗率公式: (采购量 - 销售量 - 期末库存) / 采购量 × 100%
```

**根因分类常量** (`ROOT_CAUSE_CATEGORIES`):

| 分类键 | 中文名 | 信号来源 |
|--------|--------|----------|
| `over_purchase` | 采购过多 | 预测偏差 / 安全库存偏高 |
| `poor_storage` | 存储不当 | 冷链异常 / 温控超标 |
| `processing_waste` | 加工损耗 | 出成率低 / 分拣偏差 |
| `customer_leftover` | 顾客剩余 | 退菜率高 / 泔水损耗 |

**损耗率警戒阈值** (`LOSS_THRESHOLDS`):

| SKU 类别 | 警戒阈值 | 严重阈值 |
|----------|:--------:|:--------:|
| 肉类 | 5% | 8% |
| 蔬菜 | 8% | 12% |
| 冻品 | 3% | 5% |
| 底料/蘸料 | 2% | 4% |

#### 1.3.4 ErpConnector（ERP 双向连接 N06）

```
模块路径: hotpot_platform/cloud/data_engine/erp_connector.py
模式: Adapter Pattern (适配器注册表)
```

**统一门面**: `ErpConnector`

| 方法 | 签名 | 方向 | 说明 |
|------|------|:----:|------|
| `pull_purchase_orders(store_id)` | `-> List[Dict]` | ←拉 | 拉取 ERP 采购订单 |
| `push_order_suggestion(suggestion)` | `-> ErpSyncResult` | →推 | 推送订货建议(不下单) |
| `push_inventory_status(snapshot)` | `-> ErpSyncResult` | →推 | 同步当前库存 |
| `push_loss_report(report)` | `-> ErpSyncResult` | →推 | 推送损耗报告 |

**已注册适配器**:

| 适配器名 | 类 | 目标系统 | 状态 |
|----------|-----|----------|:----:|
| `file` | FileAdapter | JSON 文件 | ✅ 开发用 |
| `mock` | MockAdapter | 内存模拟 | ✅ 测试用 |
| `rest_api` | RestApiAdapter | 通用 REST API | 🔄 待实现 |
| `hualala` | HualalaAdapter | 哗啦啦餐饮 SaaS | 📋 规划中 |
| `tflong` | TflongAdapter | 天财商龙餐饮 SaaS | 📋 规划中 |
| `kingdee` | KingdeeAdapter | 金蝶云·星辰 | 📋 规划中 |

---

### 1.4 集成桥接层

#### 1.4.1 EdgeHubClient（边缘→平台 HTTP 客户端）

```
模块路径: common/hub_client.py
特性: 自动重试 + SQLite 离线队列 + 断网存活
```

**类**: `EdgeHubClient`

```python
class EdgeHubClient:
    def __init__(
        self,
        hub_url: str,                 # 平台地址, 如 "http://192.168.2.85:8088"
        store_id: str,                # 门店 ID
        api_key: str = "",            # API Key (从环境变量读取)
        queue_db: Optional[Path] = None,  # 离线队列 DB 路径
    ) -> None: ...

    def post(self, path: str, body: Any, *, store_query: bool = True) -> bool:
        """
        POST 请求到 Event Hub。
        失败时自动入队 SQLite 离线队列，网络恢复后自动重发。

        Returns: True=成功/已入队, False=严重错误
        """

    def flush_queue(self) -> int:
        """
        尝试发送离线队列中的所有待发消息。
        Returns: 成功发送数量
        """
```

**离线队列 Schema**:
```sql
CREATE TABLE IF NOT EXISTS pending (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    path TEXT NOT NULL,          -- API 路径, 如 "/api/v1/ingest"
    query TEXT,                  -- 查询参数
    body TEXT NOT NULL,          -- JSON 请求体
    created_at TEXT DEFAULT (datetime('now'))
);
```

**重试策略**:

| 场景 | 行为 |
|------|------|
| 网络超时 (≤5s) | 立即重试 1 次 |
| 网络失败 | 入队 SQLite，后台每 30s 尝试 flush |
| 队列满 (>1000 条) | 丢弃最旧条目，记录警告日志 |
| 断网恢复 | 全量 flush，按 FIFO 顺序 |

#### 1.4.2 POS Bridge（POS 数据桥接）

```
模块路径: hotpot_platform/cloud/integrations/pos_bridge.py
数据源: 文件 / API / 模拟
输出: Event Hub POST /pos
```

**支持的数据源模式**:

| 模式 | 配置 | 说明 |
|------|------|------|
| `file` | `DEFAULT_POS_FILE` | 从 JSON 文件加载 |
| `simulated` | 基于 file + 随机漂移 | 模拟实时 POS 流 |
| `api` | `--pos-api-url` | 从外部 API 拉取 |

**POS 数据结构**:
```json
{
    "store_id": "store_yuhuan",
    "store_name": "冯校长火锅·玉环店",
    "date": "2026-07-29",
    "source": "file",
    "synced_at": "2026-07-29T13:00:00Z",
    "daily_revenue": 48500,
    "turnover_rate": 2.8,
    "dish_timeout_count": 3,
    "queue_count": 5,
    "queue_lost_rate": 0.08,
    "top_dishes": [{"name": "毛肚", "qty": 128}, ...]
}
```

---

### 1.5 告警网关层

#### 1.5.1 AlertGateway（企业微信告警网关）

```
模块路径: hotpot_platform/cloud/alert_gateway/gateway.py
通道: 企业微信 Webhook (生产) / 文件 Mock (开发)
特性: 去重 + 限流 + 推送日志 + 审计追踪
```

**类**: `AlertGateway`

```python
class AlertGateway:
    def __init__(
        self,
        db_path: Path,
        notifier: Optional[WechatNotifier] = None,
    ) -> None: ...

    def route_event(self, store_id: str, event: Dict) -> Dict:
        """
        路由事件到企业微信。
        critical → 即时推送
        warn → 批量推送(每5分钟一次)
        info → 仅记录不推送
        """

    def send_test_push(self, store_id: str) -> Dict:
        """发送测试卡片(验证 Webhook 配置)"""

    def list_acks(self, store_id: str) -> List[Dict]:
        """查询告警确认记录"""

    def route_status(self, store_id: str) -> Dict:
        """返回 Webhook 配置状态(URL 已脱敏)"""
```

**告警级别处理策略**:

| 级别 | 推送行为 | 去重窗口 | 限流 |
|------|----------|:--------:|:----:|
| `critical` | 立即推送 | 10 分钟 | 无 |
| `warn` | 批量推送(每 5min) | 30 分钟 | 20 条/小时 |
| `info` | 仅记录 | — | — |

**企业微信卡片格式**:
```markdown
### 🔴 火瞳告警 - {store_name}

**{event_type}**
{message}

📍 区域: {zone} | ⏰ 时间: {timestamp}
📊 置信度: {confidence:.0%}

[查看详情]({dashboard_url})
[确认处理]({ack_url})
```

---

### 1.6 SOP 合规引擎层

> **对应 PRD 功能**：SC01（SOP 模板库管理）、SC02（实时合规检查）、SC03（违规记录与追踪）
> **设计模式**：Strategy Pattern（检查策略可插拔）+ Observer Pattern（违规事件发布）

#### 1.6.1 SOPChecker（合规检查引擎 SC02）

```
模块路径: hotpot_platform/cloud/sop_engine/checker.py
依赖: Event Hub (事件源) + 知识库 (SOP规则) + Vision Worker (视觉证据)
触发方式: 事件驱动 (EventHub 订阅 vision/iot 事件) + 定时巡检
```

**类**: `SOPChecker`

```python
class SOPChecker:
    """SOP 合规检查引擎 — 对接 PRD SC02 实时合规检查"""

    def __init__(
        self,
        template_repo: SOPTemplateRepository,   # SOP 模板仓库
        event_store: EventStore,                # 事件存储
        alert_gateway: AlertGateway,             # 告警网关
        evidence_collector: EvidenceCollector,   # 证据收集器(可选)
    ) -> None: ...

    def check_compliance(
        self,
        store_id: str,
        zone: str,                    # front / kitchen / warehouse / dining
        evidence: Optional[Dict] = None,  # 视觉/IoT证据
        check_time: Optional[datetime] = None,
    ) -> ComplianceReport:
        """
        执行单次合规检查。

        返回 ComplianceReport:
        {
            store_id, zone, check_time,
            total_rules: int,              # 该区域适用规则总数
            passed: int,                   # 通过数
            violated: int,                 # 违规数
            skipped: int,                  # 跳过数(无证据)
            score: float,                  # 0~100 合规分数
            violations: [                  # 违规明细
                {
                    rule_id: str,          # 如 "SOP-KITCHEN-001"
                    rule_name: str,        # 如 "厨师必须佩戴口罩"
                    severity: str,         # critical / major / minor
                    evidence_ref: str,     # 证据文件路径/事件ID
                    suggested_action: str, # 建议纠正动作
                    detected_at: datetime,
                }
            ],
        }
        """

    def batch_check(
        self,
        store_id: str,
        zones: Optional[List[str]] = None,  # None=全部区域
    ) -> Dict[str, ComplianceReport]:
        """批量检查门店所有区域（或指定区域）"""

    def get_compliance_trend(
        self,
        store_id: str,
        days: int = 30,
    ) -> ComplianceTrend:
        """
        返回合规趋势:
        {
            store_id, period_days,
            daily_scores: [(date, score), ...],
            avg_score: float,
            worst_zone: str,
            improvement_pct: float,      # 较上周变化
            top_violations: [...],       # Top 5 高频违规
        }
        """
```

**检查策略注册表** (`CHECK_STRATEGIES`):

| 策略 ID | 名称 | 区域 | 证据来源 | 检查逻辑 |
|--------|------|:----:|----------|----------|
| `mask_detect` | 口罩佩戴检测 | kitchen | 视觉(YOLO人脸+口罩分类) | 厨房区域内人脸未检测到口罩 → 违规 |
| `hand_wash` | 洗手合规 | kitchen | 视觉(行为识别) + IoT(水龙头传感器) | 进入操作台前 30s 内无洗手记录 → 违规 |
| `temp_monitor` | 温控合规 | warehouse | IoT(温度传感器) | 冷链温度 > 阈值持续 > 15min → 违规 |
| `fevo_check` | FEFO 先失效先出 | warehouse | RFID + 库存快照 | 出库批次非最早过期 → 违规 |
| `table_cleanup` | 桌面清洁 | front | 视觉(桌面检测) | 顾客离桌 > 10min 未清理 → 提醒 |
| `greeting_std` | 迎宾标准 | front | 视觉(行为识别) | 顾客进门 > 30s 无迎宾 → 提醒 |

#### 1.6.2 SOPTemplateManager（SOP 模板管理 SC01）

```
模块路径: hotpot_platform/cloud/sop_engine/template_manager.py
模式: Repository Pattern + 版本控制(SemVer)
```

**类**: `SOPTemplateManager`

```python
class SOPTemplateManager:
    """SOP 模板 CRUD + 版本管理 — 对接 PRD SC01"""

    def __init__(self, db_session) -> None: ...

    def create_template(
        self,
        name: str,
        category: str,               # kitchen_hygiene / food_safety / service_std / warehouse_op
        zone: str,                    # 适用区域
        rules: List[SOPRule],         # 规则列表
        author: str,
        store_scope: Optional[str] = None,  # None=全连锁, "store_xxx"=单店
    ) -> SOPTemplate:
        """创建新模板，自动分配 v1.0.0 版本号"""

    def update_template(
        self,
        template_id: str,
        rules: Optional[List[SOPRule]] = None,
        status: Optional[str] = None,  # draft / active / archived
        updater: str = "",
    ) -> SOPTemplate:
        """更新模板，自动递增补丁版本号 (v1.0.0 → v1.0.1)"""

    def list_templates(
        self,
        category: Optional[str] = None,
        zone: Optional[str] = None,
        status: str = "active",
        page: int = 1,
        size: int = 20,
    ) -> PaginatedResult[SOPTemplate]:
        """分页查询模板列表"""

    def get_template_version_history(
        self,
        template_id: str,
    ) -> List[TemplateVersion]:
        """获取模板版本历史"""
```

**SOPRule 数据结构**:

```python
@dataclass
class SOPRule:
    rule_id: str                      # 如 "SOP-KITCHEN-001"
    name: str                         # 如 "厨师必须佩戴口罩"
    description: str                  # 自然语言描述
    severity: str                     # critical / major / minor / info
    check_strategy: str               # CHECK_STRATEGIES 中的 key
    threshold: Optional[Dict] = None  # 策略参数，如 {"min_temp_c": -18, "duration_min": 15}
    corrective_action: str            # 纠正动作指引
    evidence_required: bool = True    # 是否需要视觉/IoT证据
```

#### 1.6.3 ViolationTracker（违规记录 SC03）

```
模块路径: hotpot_platform/cloud/sop_engine/violation_tracker.py
输出: Event Hub critical/warn 事件 + sop_violations 表持久化
```

**类**: `ViolationTracker`

```python
class ViolationTracker:
    """违规记录与追踪 — 对接 PRD SC03"""

    def __init__(self, db_session, alert_gateway: AlertGateway) -> None: ...

    def record_violation(
        self,
        report: ComplianceReport,
        auto_acknowledge: bool = False,   # minor级别自动确认
    ) -> ViolationRecord:
        """
        记录违规。
        - critical/major → 自动推送告警
        - minor → 仅记录
        - 返回 ViolationRecord 含 violation_id
        """

    def acknowledge(
        self,
        violation_id: str,
        ack_by: str,
        note: str = "",
        corrective_evidence: Optional[str] = None,  # 纠正照片路径
    ) -> ViolationRecord:
        """确认违规已处理"""

    def query_violations(
        self,
        store_id: str,
        start_date: date,
        end_date: date,
        severity: Optional[str] = None,
        status: Optional[str] = None,   # open / acknowledged / resolved
        page: int = 1,
        size: int = 20,
    ) -> PaginatedResult[ViolationRecord]:
        """分页查询违规记录"""

    def getViolationStats(
        self,
        store_id: str,
        period_days: int = 30,
    ) -> ViolationStats:
        """
        返回违规统计:
        {
            total_violations, by_severity, by_category,
            avg_resolution_hours, repeat_rate,  # 同一规则重复违规率
            top_3_repeat_rules,                # 最常重复的规则
        }
        """
```

---

### 1.7 知识库检索层

> **对应 PRD 功能**：KT01（菜品知识库）、KT02（经营 Know-how 检索）
> **技术方案**：向量检索(Embedding) + 关键词检索(BM25) 混合 RRF 融合

#### 1.7.1 KnowledgeRetriever（知识检索引擎 KT01/KT02）

```
模块路径: hotpot_platform/cloud/knowledge/retriever.py
检索模式: Hybrid Search (BM25 + Vector) → RRF (Reciprocal Rank Fusion)
Embedding: text-embedding-3-small (OpenAI) / bge-large-zh (本地备选)
向量库: pgvector (PostgreSQL 扩展) / Qdrant (独立部署备选)
```

**类**: `KnowledgeRetriever`

```python
class KnowledgeRetriever:
    """知识库混合检索引擎 — 对接 PRD KT01 + KT02"""

    def __init__(
        self,
        db_pool,                       # PostgreSQL 连接池(含pgvector)
        embedding_client: EmbeddingClient,  # 向量嵌入客户端
        bm25_index: BM25Index,          # BM25 倒排索引
        rrf_k: int = 60,               # RRF 融合参数
    ) -> None: ...

    def query(
        self,
        query_text: str,
        store_id: Optional[str] = None,
        category: Optional[str] = None,  # dish / operation / supplier / safety
        top_k: int = 5,
        min_score: float = 0.3,
        hybrid_weight: float = 0.6,      # 向量权重(0~1), BM25权重=1-hybrid_weight
    ) -> KnowledgeQueryResult:
        """
        混合检索。

        返回 KnowledgeQueryResult:
        {
            query_text, store_id, category,
            results: [                    # 按 RRF 分数降序
                {
                    item_id: str,
                    title: str,
                    content: str,           # 正文片段(高亮关键词)
                    category: str,
                    source_doc: str,        # 来源文档名
                    vector_score: float,    # 向量相似度
                    bm25_score: float,      # BM25 相关性
                    rrf_score: float,       # 融合分数
                    metadata: Dict,         # 自定义元数据
                }
            ],
            total_found: int,
            query_ms: float,             # 检索耗时
        }
        """

    def dish_query(
        self,
        dish_name: str,
        intent: str = "general",        # general / recipe / pricing / pairing
        store_id: Optional[str] = None,
    ) -> DishKnowledgeResult:
        """
        菜品专项检索 (KT01)。
        返回:
        {
            dish_name, intent,
            basic_info: { category, taste_profile, allergens, ... },
            recipes: [{ name, steps, difficulty, time_min }],
            pricing_ref: { cost_range, suggested_price, margin_pct },
            pairing_suggestions: [{ dish_name, reason }],
            quality_standards: [{ spec, method, threshold }],
            source_items: [...],          # 来自知识库的原始条目
        }
        """

    def operation_query(
        self,
        question: str,
        context: Optional[str] = None,   # 上下文(如当前工序)
        store_id: Optional[str] = None,
    ) -> OperationKnowledgeResult:
        """
        经营 Know-how 检索 (KT02)。
        支持自然语言问题，如：
        - "毛肚怎么处理才能保持脆度？"
        - "底料配方比例是多少？"
        - "翻台率低怎么改善？"

        返回 OperationKnowledgeResult:
        {
            question, context,
            answer: str,                  # 生成的摘要回答
            sources: [...],               # 引用来源条目
            confidence: float,            # 回答置信度
            follow_up_questions: [str],   # 建议的追问
        }
        """

    def add_item(
        self,
        title: str,
        content: str,
        category: str,
        source_doc: str,
        metadata: Optional[Dict] = None,
        author: str = "system",
    ) -> KnowledgeItem:
        """新增知识条目（含自动向量化）"""

    def delete_item(self, item_id: str, deleted_by: str) -> bool:
        """删除知识条目（软删除）"""
```

**知识库分类体系** (`KNOWLEDGE_CATEGORIES`):

| 分类键 | 中文名 | 示例条目 | 对应PRD |
|--------|--------|----------|---------|
| `dish` | 菜品知识 | 毛肚处理、底料配方、蘸料配比 | KT01 |
| `operation` | 操作规范 | 切配标准、摆盘规范、出餐流程 | KT02 |
| `supplier` | 供应商信息 | 王总冻品、潘厨品质标准 | N05 |
| `safety` | 食品安全 | 保质期管理、冷链要求、留样规定 | SC02 |
| `service` | 服务标准 | 迎宾话术、投诉处理、加汤时机 | K系列 |
| `finance` | 经营数据 | 成本结构、定价策略、损耗基准 | N04 |

**RRF 融合算法**:

```
RRF_score(d) = Σ_{r∈R} 1 / (k + rank_r(d))

其中:
  d = 候选文档
  R = {bm25, vector} 检索结果集
  rank_r(d) = 文档 d 在检索结果集 r 中的排名(从1开始)
  k = 60 (默认平滑常数)
```

---

### 1.8 仓库 IoT 管理层

> **对应 PRD 功能**：WH01（RFID 批次追踪）、WH02（温湿度监控）、WH03（FEFO 先失效先出）、
> WH04（库存预警）、WH05（效期管理）、WH06（IoT 设备管理）
> **技术架构**：MQTT 消息采集 → Edge Agent 本地聚合 → HTTPS 批量上报 → 平台规则引擎

#### 1.8.1 RFIDTracker（批次追踪 WH01）

```
模块路径: hotpot_platform/cloud/warehouse/rfid_tracker.py
数据源: MQTT (warehouse/gateway/{store_id}/rfid) + API 手动录入
硬件: UHF RFID 读写器 (Impinj R420 或兼容) + 抗金属标签
```

**类**: `RFIDTracker`

```python
class RFIDTracker:
    """RFID 批次追踪引擎 — 对接 PRD WH01"""

    def __init__(
        self,
        db_session,
        event_hub_client: EventHubClient,
        inventory_service: InventoryService,  # 库存服务引用
    ) -> None: ...

    def track_batch(
        self,
        store_id: str,
        batch_id: str,                  # 批次号(RFID EPC或手动编码)
        items: List[RFIDItem],          # 该批次的物品清单
        operation: str,                  # receive / transfer / consume / waste / ship_out
        operator: str,
        location: str,                   # cold_room_a / shelf_b02 / prep_table
        photos: Optional[List[str]] = None,  # 收货/出库照片路径
    ) -> TrackingResult:
        """
        记录批次流转。

        返回 TrackingResult:
        {
            batch_id, store_id, operation,
            items_tracked: int,           # 追踪到的物品数
            items_expected: int,          # 应有物品数
            match_rate: float,            # 匹配率 0~1
            discrepancies: [               # 差异明细
                {
                    epc: str,
                    sku: str,
                    expected_qty: int,
                    actual_qty: int,
                    reason: str,          # missing / extra / wrong_location
                }
            ],
            ledger_entries_created: int,   # 创建的库存台账条目数
            alerts_triggered: List[str],   # 触发的告警ID列表
        }
        """

    def query_batch_trace(
        self,
        batch_id: str,
        store_id: str,
    ) -> BatchTrace:
        """
        查询批次完整追溯链。
        返回 BatchTrace:
        {
            batch_id, store_id, sku,
            supplier_name, received_at,
            current_location, current_qty,
            timeline: [                   # 时间线
                {
                    timestamp, operation, operator,
                    location, qty_change, photos,
                }
            ],
            remaining_shelf_life_days: int,
            fevo_status: str,             # normal / warning / expired
        }
        """

    def locate_item(
        self,
        epc: str,
        store_id: str,
    ) -> ItemLocation:
        """查询单个 RFID 标签的当前位置"""
```

**RFIDItem 数据结构**:

```python
@dataclass
class RFIDItem:
    epc: str                           # 电子产品码(唯一标识)
    sku: str                           # 商品编码
    batch_id: str                      # 所属批次
    quantity: float = 1.0              # 数量(kg/件)
    unit: str = "kg"                   # 单位
    production_date: Optional[date] = None  # 生产日期
    expiry_date: Optional[date] = None     # 保质期截止
```

#### 1.8.2 FEFOMonitor（FEFO 监控 WH03）

```
模块路径: hotpot_platform/cloud/warehouse/fefo_monitor.py
触发: 定时任务(每15分钟) + 库存变动事件
输出: FEFO 告警(Event Hub) + 出库建议(API)
```

**类**: `FEFOMonitor`

```python
class FEFOMonitor:
    """先失效先出(FEFO)监控引擎 — 对接 PRD WH03"""

    def __init__(self, db_session, alert_gateway: AlertGateway) -> None: ...

    def check_fevo(
        self,
        store_id: str,
        sku: Optional[str] = None,       # None=检查全部SKU
    ) -> FEVOStatus:
        """
        检查 FEFO 状态。

        返回 FEVOStatus:
        {
            store_id, checked_at,
            items_checked: int,
            items_normal: int,            # 效期充足(>7天)
            items_warning: int,           # 即将过期(1~7天)
            items_expired: int,           # 已过期
            recommendations: [            # 出库建议
                {
                    sku, batch_id,
                    expiry_date,
                    days_remaining: int,
                    on_hand_qty: float,
                    action: "consume_first" | "discard" | "discount_sale",
                    priority: int,         # 1=最紧急
                }
            ],
            overall_score: float,          # 0~100 FEFO健康分
        }
        """

    def generate_pick_list(
        self,
        store_id: str,
        order_items: List[OrderItem],     # 需要拣货的商品清单
    ) -> PickList:
        """
        生成 FEFO 拣货清单。
        优先选择最早过期的批次，确保先失效先出。

        返回 PickList:
        {
            store_id, generated_at,
            picks: [
                {
                    sku, required_qty,
                    allocations: [         # 分配明细(按FEFO排序)
                        { batch_id, qty, expiry_date, location },
                    ]
                }
            ],
            warnings: [...]               # 无法满足FEFO的警告
        }
        """
```

#### 1.8.3 IoTMonitor（温湿度监控 WH02/WH06）

```
模块路径: hotpot_platform/cloud/warehouse/iot_monitor.py
数据源: MQTT (warehouse/sensor/{store_id}/{device_id}/telemetry)
设备: DS18B20(温度) + SHT30(温湿度) + AM2302(环境) + RFID Gateway
```

**类**: `IoTMonitor`

```python
class IoTMonitor:
    """仓库 IoT 监控引擎 — 对接 PRD WH02 + WH06"""

    def __init__(
        self,
        db_session,
        mqtt_client: MQTTClient,
        alert_gateway: AlertGateway,
    ) -> None: ...

    def ingest_telemetry(
        self,
        store_id: str,
        device_id: str,
        readings: List[IoTReading],
    ) -> IngestResult:
        """
        接收 IoT 遥测数据并写入数据库。
        同时执行阈值检查，超限即触发告警。
        """

    def get_device_status(
        self,
        store_id: str,
        device_id: Optional[str] = None,
    ) -> List[DeviceStatus]:
        """
        返回设备状态:
        {
            device_id, device_type, location,
            last_reading_at,
            current_temp_c, current_humidity_pct,
            battery_pct, signal_rssi,
            status: online / offline / alarm / maintenance,
            threshold_config: {...},
        }
        """

    def get_temperature_timeline(
        self,
        store_id: str,
        device_id: str,
        start: datetime,
        end: datetime,
        interval_minutes: int = 5,
    ) -> TemperatureTimeline:
        """获取温湿度时间线（用于冷链合规报告）"""

    def configure_threshold(
        self,
        device_id: str,
        temp_min_c: float,
        temp_max_c: float,
        humidity_min_pct: Optional[float] = None,
        humidity_max_pct: Optional[float] = None,
        alarm_duration_sec: int = 900,     # 持续超限多久告警(默认15分钟)
        configured_by: str = "",
    ) -> DeviceThresholdConfig:
        """配置单个设备的监控阈值"""
```

**IoT 阈值默认配置** (`DEFAULT_THRESHOLDS`):

| 区域 | 设备类型 | 温度下限 | 温度上限 | 湿度下限 | 湿度上限 | 告警延迟 |
|------|:--------:|:--------:|:--------:|:--------:|:--------:|:--------:|
| 冷藏间 | 温湿度传感器 | 0°C | 4°C | 75% | 90% | 15min |
| 冷冻间 | 温湿度传感器 | -22°C | -16°C | — | — | 15min |
| 常温仓 | 温湿度传感器 | 15°C | 28°C | 40% | 70% | 30min |
| 后厨 | 温湿度传感器 | 18°C | 30°C | 40% | 70% | 30min |

#### 1.8.4 InventoryAlertor（库存预警 WH04）

```
模块路径: hotpot_platform/cloud/warehouse/inventory_alertor.py
触发: 库存快照变更 + 定时任务(每日9:00)
输出: Event Hub warn/critical 事件
```

**类**: `InventoryAlertor`

```python
class InventoryAlertor:
    """库存预警引擎 — 对接 PRD WH04"""

    def __init__(self, db_session, alert_gateway: AlertGateway) -> None: ...

    def check_stock_levels(
        self,
        store_id: str,
    ) -> StockAlertReport:
        """
        检查库存水位。

        返回 StockAlertReport:
        {
            store_id, checked_at,
            alerts: [
                {
                    sku, sku_name, category,
                    current_qty, unit,
                    safety_stock_level,          # 安全库存
                    days_of_stock: float,        # 可销售天数
                    daily_avg_consumption: float,# 日均消耗
                    alert_type: str,             # stockout / overstock / slow_moving / expiring
                    urgency: str,                # critical / high / medium / low
                    suggested_order_qty: float,  # 建议订货量
                    estimated_stockout_date: date, # 预计断货日期
                }
            ],
            summary: {
                critical_count, high_count, medium_count,
                total_at_risk_sku,
            }
        }
        """

    def configure_alert_rule(
        self,
        sku: str,
        store_id: str,
        rule_type: str,                  # stockout / overstock / expiring
        threshold_value: float,
        unit: str = "days",              # days / qty / pct
        enabled: bool = True,
    ) -> AlertRule:
        """配置单品级预警规则"""
```

---

### 1.9 会员营销层

> **对应 PRD 功能**：FH01（会员视觉识别）、FH02（积分系统）、FH03（精准营销）、FH04（会员画像）
> **隐私原则**：人脸特征不上云，仅存储本地 Hash + 会员标签；符合《个人信息保护法》

#### 1.9.1 MemberRecognizer（会员识别 FH01）

```
模块路径: edge/front_hall/member/recognizer.py (边缘侧)
        hotpot_platform/cloud/member/service.py (平台侧)
技术: FaceNet (特征提取) + Faiss (向量索引) + 差分隐私(Hash上传)
数据流: 边缘提取特征 → 本地匹配 → Hash+标签上传平台 → 平台汇总画像
```

**类(边缘侧)**: `EdgeMemberRecognizer`

```python
class EdgeMemberRecognizer:
    """边缘侧会员识别 — 人脸特征提取与本地匹配"""

    def __init__(
        self,
        model_path: Path,               # FaceNet ONNX 模型
        index_path: Path,               # Faiss 本地索引文件
        hub_client: EdgeHubClient,
        recognition_threshold: float = 0.65,  # 识别置信度阈值
    ) -> None: ...

    def recognize_from_frame(
        self,
        frame: np.ndarray,
        camera_id: str,
        store_id: str,
    ) -> RecognitionResult:
        """
        从摄像头帧中识别会员。

        返回 RecognitionResult:
        {
            camera_id, store_id, timestamp,
            faces_detected: int,
            members: [
                {
                    member_hash: str,        # SHA-256 人脸特征Hash(用于上传)
                    local_member_id: str,    # 本地会员ID(如有匹配)
                    confidence: float,       # 识别置信度 0~1
                    bbox: [x,y,w,h],         # 人脸框坐标
                    is_known: bool,          # 是否已知会员
                }
            ],
            processing_ms: float,
        }
        注意: 原始人脸特征向量不离开边缘设备，仅上传 Hash + 标签
        """

    def enroll_member(
        self,
        member_id: str,
        face_frames: List[np.ndarray],   # 多帧人脸(用于特征平均)
        member_tags: List[str],          # 标签: ["VIP","偏好麻辣","3人用餐"]
    ) -> EnrollmentResult:
        """注册新会员（本地建索引 + 上传Hash+标签到平台）"""

    def sync_member_index(
        self,
        delta_members: List[MemberDelta],  # 平台下发的增量会员数据
    ) -> SyncResult:
        """同步平台下发的会员索引增量"""
```

**类(平台侧)**: `MemberService`

```python
class MemberService:
    """平台侧会员服务 — 画像管理与营销对接"""

    def __init__(self, db_session) -> None: ...

    def record_visit(
        self,
        store_id: str,
        member_hash: str,
        visit_data: VisitData,
    ) -> MemberVisit:
        """记录会员到店访问"""

    def get_member_profile(
        self,
        member_id: str,
        store_id: Optional[str] = None,
    ) -> MemberProfile:
        """
        获取会员画像 (FH04)。
        返回 MemberProfile:
        {
            member_id, avatar_hash,
            level: str,                    # ordinary / gold / platinum / diamond
            points_balance: int,
            total_visits: int,
            total_spend: float,
            avg_spend_per_visit: float,
            favorite_dishes: [...],         # Top 5 偏好菜品
            preferred_time_slots: [...],    # 常用时段
            tags: [...],                    # 系统标签 + 人工标签
            last_visit_at,
            visit_frequency_days: float,    # 平均到店间隔(天)
            churn_risk: str,                # low / medium / high
            next_visit_predicted: date,     # 预测下次到店日期
        }
        """

    def segment_members(
        self,
        store_id: str,
        criteria: SegmentCriteria,         # 分群条件
    ) -> MemberSegment:
        """会员分群 (FH03 精准营销基础)"""
```

#### 1.9.2 LoyaltyEngine（积分系统 FH02）

```
模块路径: hotpot_platform/cloud/member/loyalty.py
规则引擎: 可配置的积分规则 DSL (Domain Specific Language)
```

**类**: `LoyaltyEngine`

```python
class LoyaltyEngine:
    """积分引擎 — 对接 PRD FH02"""

    def __init__(
        self,
        db_session,
        rule_loader: PointsRuleLoader,     # 积分规则加载器
    ) -> None: ...

    def calculate_points(
        self,
        member_id: str,
        transaction: Transaction,          # 消费交易
    ) -> PointsDelta:
        """
        计算交易积分。

        返回 PointsDelta:
        {
            member_id, transaction_id,
            base_points: int,               # 基础积分(消费金额×倍率)
            bonus_points: int,              # 奖励积分(活动/首单/生日等)
            deduction_points: int,          # 抵扣积分(如使用)
            net_points: int,                # 净得积分
            breakdown: [                    # 明细
                { rule_name, points, reason },
            ],
        }
        """

    def redeem_points(
        self,
        member_id: str,
        points_to_redeem: int,
        reward_type: str,                   # cashback / dish / coupon
        reward_spec: Dict,
        redeemed_by: str,
    ) -> RedemptionResult:
        """积分兑换"""

    def get_points_statement(
        self,
        member_id: str,
        start_date: date,
        end_date: date,
    ) -> PointsStatement:
        """获取积分流水账单"""

    def configure_points_rule(
        self,
        rule_name: str,
        rule_config: PointsRuleConfig,      # DSL 格式规则
        effective_from: date,
        effective_until: Optional[date] = None,
    ) -> PointsRule:
        """
        配置积分规则 (DSL 示例):
        {
            "trigger": "transaction",
            "condition": { "amount_gte": 100 },
            "award": { "base": "amount * 1", "bonus": 50 },
            "description": "每消费1元积1分，满100额外送50分"
        }
        """
```

**会员等级体系** (`MEMBER_TIERS`):

| 等级 | 名称 | 升级条件(年消费) | 积分倍率 | 权益 |
|:----:|------|:----------------:|:--------:|------|
| L1 | 普通会员 | 注册即可 | 1.0x | 基础积分 |
| L2 | 金卡会员 | ≥ ¥3,000 | 1.2x | 生日双倍 + 优先排队 |
| L3 | 白金会员 | ≥ ¥10,000 | 1.5x | 金卡权益 + 免费菜品/月 |
| L4 | 钻石会员 | ≥ ¥30,000 | 2.0x | 白金权益 + 专属包间 + 财务授权 |

#### 1.9.3 MarketingEngine（精准营销 FH03）

```
模块路径: hotpot_platform/cloud/member/marketing.py
通道: 企业微信(员工通知) + 短信(会员触达) + App Push(未来)
合规: 遵守《个人信息保护法》+ 《广告法》，支持一键退订
```

**类**: `MarketingEngine`

```python
class MarketingEngine:
    """精准营销引擎 — 对接 PRD FH03"""

    def __init__(
        self,
        db_session,
        member_service: MemberService,
        loyalty_engine: LoyaltyEngine,
        channel_adapters: Dict[str, ChannelAdapter],  # 通道适配器
    ) -> None: ...

    def create_campaign(
        self,
        name: str,
        campaign_type: str,             # retention / acquisition / reactivation / promotion
        target_segment: SegmentCriteria,
        content: CampaignContent,
        schedule: CampaignSchedule,
        budget: Optional[CampaignBudget] = None,
        created_by: str = "",
    ) -> Campaign:
        """创建营销活动"""

    def evaluate_campaign(
        self,
        campaign_id: str,
    ) -> CampaignEvaluation:
        """
        评估营销效果。
        返回 CampaignEvaluation:
        {
            campaign_id, name, type,
            target_count, sent_count, delivered_count,
            open_rate, click_rate, conversion_rate,
            revenue_attributed: float,
            cost_per_acquisition: float,
            roi: float,
        }
        """

    def generate_personalized_offer(
        self,
        member_id: str,
        store_id: str,
        context: OfferContext,            # 场景: 到店/生日/流失预警/节日
    ) -> PersonalizedOffer:
        """
        生成个性化优惠。
        基于会员画像 + 当前场景 + 库存状态(如某SKU临期) 动态生成
        """
```

---

## §2 数据库 Schema 设计

### 2.1 ER 关系总览

```
┌─────────────┐     ┌─────────────┐     ┌──────────────────┐
│  stores     │     │  devices    │     │  events          │
│  (门店)     │     │  (设备)     │     │  (事件流)         │
├─────────────┤     ├─────────────┤     ├──────────────────┤
│ PK store_id │──┐   │ PK device_id│     │ PK event_id      │
│  name       │  │   │ FK store_id│──┐   │ FK store_id      │
│  config     │  └──▶│  type      │  │   │  level           │
│  created_at │     │  status    │  └──▶│  source          │
└─────────────┘     │  last_seen │     │  payload (JSON)  │
                   └─────────────┘     │  created_at      │
                                       └──────────────────┘

┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│  sales_daily     │  │ inventory_ledger │  │ inventory_snapshot│
│  (日销量)        │  │  (库存流水)      │  │  (库存快照)      │
├──────────────────┤  ├──────────────────┤  ├──────────────────┤
│ PK (store_id,   │  │ PK (store_id,   │  │ PK (store_id,    │
│     business_   │  │     sku, batch, │  │     sku)         │
│     date, sku)  │  │     type, ts)   │  │  on_hand_qty     │
│  qty_sold       │  │  qty_change     │  │  in_transit_qty  │
│  unit_price     │  │  unit_cost      │  │  shelf_life_days │
│  hour_dist(JSON)│  │  movement_type  │  │  earliest_expiry │
└──────────────────┘  └──────────────────┘  └──────────────────┘

┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│  sales_forecast  │  │ order_suggestion │  │ supplier_scorecard│
│  (预测结果)      │  │  (订货建议)      │  │  (供应商评分卡)  │
├──────────────────┤  ├──────────────────┤  ├──────────────────┤
│ PK (store_id,   │  │ PK (store_id,   │  │ PK (store_id,    │
│     sku, date,  │  │     sku, gen_at)│  │     supplier,sku)│
│     model_ver)  │  │  suggested_qty   │  │  total_score     │
│  predicted_qty  │  │  urgency         │  │  score_level     │
│  confidence     │  │  status          │  │  avg_variance_pct│
└──────────────────┘  └──────────────────┘  └──────────────────┘

┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│  receiving_      │  │  tasks           │  │  sop_assignments │
│  batches         │  │  (任务)          │  │  (SOP分配)       │
├──────────────────┤  ├──────────────────┤  ├──────────────────┤
│ PK id            │  │ PK task_id       │  │ PK assignment_id │
│ FK store_id      │  │ FK store_id      │  │ FK store_id      │
│  supplier_name   │  │  type            │  │  sop_id         │
│  sku             │  │  status          │  │  assignee       │
│  weight_kg       │  │  assignee        │  │  status         │
│  vlm_grade       │  │  due_at          │  │  due_at         │
│  temp_c          │  │  completed_at    │  └──────────────────┘
└──────────────────┘  └──────────────────┘

┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│  users           │  │  sop_templates   │  │  sop_violations  │
│  (用户认证)      │  │  (SOP模板)       │  │  (违规记录)      │
├──────────────────┤  ├──────────────────┤  ├──────────────────┤
│ PK user_id       │  │ PK template_id   │  │ PK violation_id  │
│  username(UNIQUE)│  │ FK store_scope   │  │ FK store_id      │
│  password_hash   │  │ category / zone  │  │ FK template_id   │
│  role            │  │ version / status │  │ severity / zone  │
│  store_id        │  │ rules (JSON)     │  │ status           │
│  is_active       │  │ author           │  │ detected_at      │
└──────────────────┘  └──────────────────┘  └──────────────────┘

┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│  knowledge_base  │  │  members         │  │  org_structure   │
│  (知识库)        │  │  (会员)          │  │  (组织架构)      │
├──────────────────┤  ├──────────────────┤  ├──────────────────┤
│ PK item_id       │  │ PK member_id     │  │ PK org_node_id   │
│  title / content │  │ face_hash(UNIQUE)│  │ FK parent_id     │
│  category        │  │ phone / level    │  │ node_type / code │
│  embedding(VEC)  │  │ points_balance   │  │ manager_user_id  │
│  tags[]          │  │ total_spend      │  │ level / name     │
│  source_doc      │  │ churn_risk       │  │ is_active        │
└──────────────────┘  └──────────────────┘  └──────────────────┘

┌──────────────────┐  ┌──────────────────┐
│  audit_logs      │  │  alerts          │
│  (操作审计)      │  │  (告警记录)      │
├──────────────────┤  ├──────────────────┤
│ PK log_id(SERIAL)│  │ PK alert_id      │
│ FK user_id       │  │ store_id         │
│ action           │  │ alert_type       │
│ target_type/id   │  │ level / source   │
│ old/new_value    │  │ status           │
│ ip_address       │  │ push_channels    │
│ result           │  │ created_at       │
└──────────────────┘  └──────────────────┘
```

### 2.2 核心表 DDL

#### 表 1: events（事件流）

```sql
CREATE TABLE IF NOT EXISTS events (
    event_id    TEXT PRIMARY KEY,          -- UUID v4
    store_id    TEXT    NOT NULL,          -- 门店 ID
    level       TEXT    NOT NULL,          -- critical / warn / info
    source      TEXT    NOT NULL,          -- vision / iot / pos / system
    payload     TEXT    NOT NULL,          -- JSON 事件体
    created_at  TEXT    NOT NULL           -- ISO-8601 UTC
);

CREATE INDEX IF NOT EXISTS idx_events_store ON events(store_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_events_level ON events(level);
CREATE INDEX IF NOT EXISTS idx_events_source ON events(source);
```

#### 表 2: sales_daily（日销量）

```sql
CREATE TABLE IF NOT EXISTS sales_daily (
    id              SERIAL PRIMARY KEY,
    store_id        TEXT    NOT NULL,
    business_date   DATE    NOT NULL,
    sku             TEXT    NOT NULL,
    sku_name        TEXT,
    category        TEXT,                      -- 肉类/蔬菜/冻品/底料/蘸料
    qty_sold        REAL    NOT NULL DEFAULT 0,
    unit            TEXT    DEFAULT '份',
    unit_price      REAL,
    revenue         REAL,
    hour_dist       TEXT,                      -- JSON: {"11": 12, "12": 28, ...}
    source          TEXT    DEFAULT 'pos',     -- pos / erp / manual
    synced_at       TIMESTAMPTZ NOT NULL,

    UNIQUE(store_id, business_date, sku)
);

CREATE INDEX IF NOT EXISTS idx_sales_store_date ON sales_daily(store_id, business_date);
CREATE INDEX IF NOT EXISTS idx_sales_sku ON sales_daily(sku);
CREATE INDEX IF NOT EXISTS idx_sales_category ON sales_daily(category);
```

#### 表 3: inventory_ledger（库存台账）

```sql
CREATE TABLE IF NOT EXISTS inventory_ledger (
    id              SERIAL PRIMARY KEY,
    store_id        TEXT    NOT NULL,
    sku             TEXT    NOT NULL,
    batch_id        TEXT,                       -- 批次号 (RFID/手动录入)
    movement_type   TEXT    NOT NULL,           -- stock_in / stock_out / adjust / waste / transfer
    qty_change      REAL    NOT NULL,           -- 正=入库, 负=出库
    qty_after       REAL,                       -- 变动后余额
    unit            TEXT    DEFAULT 'kg',
    unit_cost       REAL,                       -- 单价 (¥/kg)
    reason          TEXT,                       -- 备注
    ref_type        TEXT,                       -- 关联类型: po / so / waste / adjust
    ref_id          TEXT,                       -- 关联 ID
    operator        TEXT,                       -- 操作人
    recorded_at     TIMESTAMPTZ NOT NULL,

    UNIQUE(store_id, sku, batch_id, movement_type, recorded_at)
);

CREATE INDEX IF NOT EXISTS idx_inv_store_sku ON inventory_ledger(store_id, sku);
CREATE INDEX IF NOT EXISTS idx_inv_batch ON inventory_ledger(batch_id);
CREATE INDEX IF NOT EXISTS idx_inv_type ON inventory_ledger(movement_type);
CREATE INDEX IF NOT EXISTS idx_inv_date ON inventory_ledger(recorded_at);
```

#### 表 4: inventory_snapshot（库存快照）

```sql
CREATE TABLE IF NOT EXISTS inventory_snapshot (
    store_id              TEXT    NOT NULL,
    sku                   TEXT    NOT NULL,
    on_hand_qty           REAL    NOT NULL DEFAULT 0,
    in_transit_qty        REAL    DEFAULT 0,
    unit                  TEXT    DEFAULT 'kg',
    avg_daily_consumption REAL,
    shelf_life_days       INTEGER,
    earliest_expiry       DATE,
    last_received_at      TIMESTAMPTZ,
    last_consumed_at      TIMESTAMPTZ,
    updated_at            TIMESTAMPTZ NOT NULL,

    PRIMARY KEY (store_id, sku)
);
```

#### 表 5: sales_forecast（预测结果）

```sql
CREATE TABLE IF NOT EXISTS sales_forecast (
    id              SERIAL PRIMARY KEY,
    store_id        TEXT    NOT NULL,
    sku             TEXT    NOT NULL,
    forecast_date   DATE    NOT NULL,
    predicted_qty   REAL    NOT NULL,
    confidence      REAL,                          -- 0~1
    lower_bound     REAL,
    upper_bound     REAL,
    model_version   TEXT,                           -- L1-rule / L2-sarima / L3-lgb / L4-llm
    features_used   TEXT,                           -- JSON
    generated_at    TIMESTAMPTZ NOT NULL,

    UNIQUE(store_id, sku, forecast_date, model_version)
);

CREATE INDEX IF NOT EXISTS idx_forecast_store_date ON sales_forecast(store_id, forecast_date);
CREATE INDEX IF NOT EXISTS idx_forecast_model ON sales_forecast(model_version);
```

#### 表 6: order_suggestion（订货建议）

```sql
CREATE TABLE IF NOT EXISTS order_suggestion (
    id              SERIAL PRIMARY KEY,
    store_id        TEXT    NOT NULL,
    sku             TEXT    NOT NULL,
    suggested_qty   REAL    NOT NULL,
    unit            TEXT    DEFAULT 'kg',
    current_stock   REAL,
    safety_stock    REAL,
    forecast_demand REAL,
    lead_time_days  INTEGER DEFAULT 1,
    supplier        TEXT,
    urgency         TEXT    DEFAULT 'normal',    -- urgent / normal / low
    reason          TEXT,
    status          TEXT    DEFAULT 'pending',   -- pending / approved / rejected / ordered
    approved_by     TEXT,
    approved_at     TIMESTAMPTZ,
    generated_at    TIMESTAMPTZ NOT NULL,

    UNIQUE(store_id, sku, generated_at)
);

CREATE INDEX IF NOT EXISTS idx_order_store ON order_suggestion(store_id);
CREATE INDEX IF NOT EXISTS idx_order_status ON order_suggestion(status);
CREATE INDEX IF NOT EXISTS idx_order_urgency ON order_suggestion(urgency);
```

#### 表 7: supplier_scorecard（供应商评分卡）

```sql
CREATE TABLE IF NOT EXISTS supplier_scorecard (
    id                  SERIAL PRIMARY KEY,
    store_id            TEXT,
    supplier_name       TEXT    NOT NULL,
    sku                 TEXT,
    total_batches       INTEGER DEFAULT 0,
    avg_variance_pct    REAL,                        -- 平均短重率 (%)
    avg_yield_rate      REAL,                        -- 平均出成率 (%)
    quality_grade_dist  TEXT,                        -- JSON: {"A":80,"B":15,"C":5}
    avg_price           REAL,
    price_stability     REAL,                        -- 价格波动率
    on_time_rate        REAL,                        -- 准时交货率 (%)
    reject_rate         REAL,                        -- 拒收率 (%)
    total_score         REAL,                        -- 综合评分 0~100
    score_level         TEXT,                        -- A / B / C / D
    last_evaluated_at   TIMESTAMPTZ NOT NULL,

    UNIQUE(store_id, supplier_name, sku)
);

CREATE INDEX IF NOT EXISTS idx_supplier_name ON supplier_scorecard(supplier_name);
CREATE INDEX IF NOT EXISTS idx_supplier_score ON supplier_scorecard(total_score);
```

#### 表 8: receiving_batches（收货批次）

```sql
CREATE TABLE IF NOT EXISTS receiving_batches (
    id              SERIAL PRIMARY KEY,
    store_id        TEXT    NOT NULL,
    batch_id        TEXT    UNIQUE NOT NULL,        -- 收货批次号
    supplier_name   TEXT    NOT NULL,
    po_number       TEXT,                          -- 采购订单号
    received_at     TIMESTAMPTZ NOT NULL,
    received_by     TEXT,                          -- 收货员
    status          TEXT    DEFAULT 'pending',     -- pending / checked / signed / rejected
    total_items     INTEGER DEFAULT 0,
    total_weight_kg REAL    DEFAULT 0,
    vlm_reviewed    BOOLEAN DEFAULT FALSE,
    signature_image TEXT,                          -- 电子签名(Base64)
    notes           TEXT
);

CREATE INDEX IF NOT EXISTS idx_receiving_store ON receiving_batches(store_id);
CREATE INDEX IF NOT EXISTS idx_receiving_status ON receiving_batches(status);
CREATE INDEX IF NOT EXISTS idx_receiving_date ON receiving_batches(received_at);
```

#### 表 9: tasks（任务）

```sql
CREATE TABLE IF NOT EXISTS tasks (
    id              SERIAL PRIMARY KEY,
    task_id         TEXT    UNIQUE NOT NULL,        -- 业务 ID (如 "TASK-20260729-001")
    store_id        TEXT    NOT NULL,
    type            TEXT    NOT NULL,               -- waste_daily / restock / weekly / custom
    title           TEXT    NOT NULL,
    description     TEXT,
    status          TEXT    DEFAULT 'open',        -- open / in_progress / completed / cancelled / overdue
    priority        TEXT    DEFAULT 'normal',       -- urgent / high / normal / low
    assignee        TEXT,                          -- 责任人
    assigner        TEXT,                          -- 指派人
    due_at          TIMESTAMPTZ,
    completed_at    TIMESTAMPTZ,
    payload         TEXT,                          -- JSON 任务详情
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_tasks_store ON tasks(store_id);
CREATE INDEX IF NOT EXISTS idx_tasks_status ON tasks(status);
CREATE INDEX IF NOT EXISTS idx_tasks_type ON tasks(type);
CREATE INDEX IF NOT EXISTS idx_tasks_due ON tasks(due_at);
CREATE INDEX IF NOT EXISTS idx_tasks_assignee ON tasks(assignee);
```

#### 表 10: device_registry（设备注册）

```sql
CREATE TABLE IF NOT EXISTS device_registry (
    device_id       TEXT PRIMARY KEY,
    store_id        TEXT    NOT NULL,
    device_type     TEXT    NOT NULL,              -- ipc / iot_sensor / jetson / edge_box
    model           TEXT,                          -- 型号
    firmware_ver    TEXT,
    status          TEXT    DEFAULT 'online',      -- online / offline / maintenance
    config          TEXT    NOT NULL,              -- JSON 设备配置
    last_heartbeat  TIMESTAMPTZ,
    last_seen       TIMESTAMPTZ,
    updated_at      TIMESTAMPTZ NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_device_store ON device_registry(store_id);
CREATE INDEX IF NOT EXISTS idx_device_type ON device_registry(device_type);
CREATE INDEX IF NOT EXISTS idx_device_status ON device_registry(status);
```

#### 表 11: users（用户认证）

> **对应功能**：§3.2 RBAC 认证授权、JWT Token 签发
> **安全要求**：密码使用 bcrypt 哈希，禁止明文存储

```sql
CREATE TABLE IF NOT EXISTS users (
    user_id         TEXT PRIMARY KEY,          -- UUID v4
    username        TEXT    NOT NULL UNIQUE,
    password_hash   TEXT    NOT NULL,           -- bcrypt (cost=12)
    display_name    TEXT    NOT NULL,
    role            TEXT    NOT NULL DEFAULT 'staff',  -- 见RBAC角色矩阵
    phone           TEXT    UNIQUE,
    avatar_url      TEXT,
    store_id        TEXT,                       -- 门店权限(Null=全连锁)
    is_active       BOOLEAN NOT NULL DEFAULT TRUE,
    last_login_at   TIMESTAMPTZ,
    password_changed_at TIMESTAMPTZ NOT NULL,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_users_role ON users(role);
CREATE INDEX IF NOT EXISTS idx_users_store ON users(store_id);
CREATE INDEX IF NOT EXISTS idx_users_active ON users(is_active);
```

#### 表 12: sop_templates（SOP 模板库）

> **对应功能**：SC01 SOP 模板管理、SC02 合规检查、SC03 违规追踪

```sql
CREATE TABLE IF NOT EXISTS sop_templates (
    template_id     TEXT PRIMARY KEY,          -- UUID v4
    name            TEXT    NOT NULL,          -- 模板名称
    category        TEXT    NOT NULL,          -- kitchen_hygiene / food_safety / service_std / warehouse_op
    zone            TEXT    NOT NULL,          -- front / kitchen / warehouse / dining / all
    version         TEXT    NOT NULL DEFAULT '1.0.0',  -- SemVer
    status          TEXT    NOT NULL DEFAULT 'draft',   -- draft / active / archived
    rules           TEXT    NOT NULL,          -- JSON: SOPRule 列表
    store_scope     TEXT,                       -- Null=全连锁 / store_xxx=单店适用
    author          TEXT    NOT NULL,
    parent_version  TEXT,                       -- 上版本ID(用于版本链)
    is_active       BOOLEAN NOT NULL DEFAULT TRUE,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_sop_cat ON sop_templates(category);
CREATE INDEX IF NOT EXISTS idx_sop_zone ON sop_templates(zone);
CREATE INDEX IF NOT EXISTS idx_sop_status ON sop_templates(status);
CREATE INDEX IF NOT EXISTS idx_sop_store ON sop_templates(store_scope);
```

#### 表 13: sop_violations（违规记录）

> **对应功能**：SC03 违规记录与追踪、告警推送

```sql
CREATE TABLE IF NOT EXISTS sop_violations (
    violation_id    TEXT PRIMARY KEY,          -- UUID v4
    store_id        TEXT    NOT NULL,
    template_id     TEXT    NOT NULL REFERENCES sop_templates(template_id),
    rule_id         TEXT    NOT NULL,          -- 如 "SOP-KITCHEN-001"
    rule_name       TEXT    NOT NULL,
    severity        TEXT    NOT NULL,          -- critical / major / minor / info
    zone            TEXT    NOT NULL,
    evidence_ref    TEXT,                       -- 事件ID或文件路径
    detected_at     TIMESTAMPTZ NOT NULL,
    status          TEXT    NOT NULL DEFAULT 'open',  -- open / acknowledged / resolved
    acknowledged_by TEXT,
    acknowledged_at TIMESTAMPTZ,
    resolved_by     TEXT,
    resolved_at     TIMESTAMPTZ,
    corrective_note TEXT,
    corrective_evidence TEXT,                  -- 纠正照片路径
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_viol_store ON sop_violations(store_id);
CREATE INDEX IF NOT EXISTS idx_viol_severity ON sop_violations(severity);
CREATE INDEX IF NOT EXISTS idx_viol_status ON sop_violations(status);
CREATE INDEX IF NOT EXISTS idx_viol_detected ON sop_violations(detected_at DESC);
CREATE INDEX IF NOT EXISTS idx_viol_rule ON sop_violations(rule_id);
```

#### 表 14: knowledge_base（知识库）

> **对应功能**：KT01 菜品知识库、KT02 经营 Know-how 检索

```sql
CREATE TABLE IF NOT EXISTS knowledge_base (
    item_id         TEXT PRIMARY KEY,          -- UUID v4
    title           TEXT    NOT NULL,
    content         TEXT    NOT NULL,          -- 正文(Markdown)
    category        TEXT    NOT NULL,          -- dish / operation / supplier / safety / service / finance
    source_doc      TEXT    NOT NULL,          -- 来源文档名
    embedding       VECTOR(1536),              -- pgvector 向量(OpenAI text-embedding-3-small)
    metadata        TEXT,                       -- JSON 自定义元数据
    tags            TEXT[],                     -- 标签数组
    author          TEXT    DEFAULT 'system',
    is_deleted      BOOLEAN NOT NULL DEFAULT FALSE,
    version         INTEGER NOT NULL DEFAULT 1,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- pgvector HNSW 索引(近似最近邻检索)
CREATE INDEX IF NOT EXISTS idx_knowledge_vec ON knowledge_base USING hnsw (embedding vector_cosine_ops);
CREATE INDEX IF NOT EXISTS idx_knowledge_cat ON knowledge_base(category);
CREATE INDEX IF NOT EXISTS idx_knowledge_tags ON knowledge_base USING gin(tags);
CREATE INDEX IF NOT EXISTS idx_knowledge_deleted ON knowledge_base(is_deleted);
```

#### 表 15: members（会员）

> **对应功能**：FH01 会员识别、FH04 会员画像
> **隐私**：不存储原始人脸特征，仅存储 SHA-256 Hash

```sql
CREATE TABLE IF NOT EXISTS members (
    member_id       TEXT PRIMARY KEY,          -- UUID v4
    face_hash       TEXT    UNIQUE,            -- SHA-256 人脸特征Hash(隐私保护)
    phone           TEXT    UNIQUE,
    wechat_openid   TEXT    UNIQUE,
    display_name    TEXT    NOT NULL,
    avatar_url      TEXT,
    level           TEXT    NOT NULL DEFAULT 'ordinary',  -- ordinary / gold / platinum / diamond
    points_balance  INTEGER NOT NULL DEFAULT 0,
    total_spend     REAL    NOT NULL DEFAULT 0,
    total_visits    INTEGER NOT NULL DEFAULT 0,
    favorite_dishes TEXT,                       -- JSON: Top 偏好菜品
    preferred_slots TEXT,                       -- JSON: 常用时段
    tags            TEXT[],                     -- 系统标签 + 人工标签
    churn_risk      TEXT    DEFAULT 'low',      -- low / medium / high
    home_store_id   TEXT,                       -- 常去门店
    is_active       BOOLEAN NOT NULL DEFAULT TRUE,
    consent_marketing BOOLEAN NOT NULL DEFAULT FALSE,  -- 营销同意
    last_visit_at   TIMESTAMPTZ,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_members_phone ON members(phone);
CREATE INDEX IF NOT EXISTS idx_members_level ON members(level);
CREATE INDEX IF NOT EXISTS idx_members_store ON members(home_store_id);
CREATE INDEX IF NOT EXISTS idx_members_face ON members(face_hash);
```

#### 表 16: points_transactions（积分流水）

> **对应功能**：FH02 积分系统

```sql
CREATE TABLE IF NOT EXISTS points_transactions (
    txn_id          TEXT PRIMARY KEY,          -- UUID v4
    member_id       TEXT    NOT NULL REFERENCES members(member_id),
    txn_type        TEXT    NOT NULL,          -- earn / redeem / expire / adjust / bonus
    points          INTEGER NOT NULL,          -- 正=获得, 负=消耗
    balance_after   INTEGER NOT NULL,          -- 交易后余额
    ref_type        TEXT,                       -- transaction / campaign / manual / birthday
    ref_id          TEXT,                       -- 关联交易/活动ID
    description     TEXT,
    created_by      TEXT,                       -- 系统(system) / 操作员(user_id)
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_points_member ON points_transactions(member_id);
CREATE INDEX IF NOT EXISTS idx_points_type ON points_transactions(txn_type);
CREATE INDEX IF NOT EXISTS idx_points_date ON points_transactions(created_at DESC);
```

#### 表 17: org_structure（组织架构）

> **对应功能**：H01-H14 连锁管控（多店/区域/总部层级）

```sql
CREATE TABLE IF NOT EXISTS org_structure (
    org_node_id     TEXT PRIMARY KEY,          -- UUID v4
    parent_id       TEXT    REFERENCES org_structure(org_node_id),
    node_type       TEXT    NOT NULL,          -- headquarters / region / store / department / team
    name            TEXT    NOT NULL,
    code            TEXT    UNIQUE,            -- 组织编码如 "HQ" "REGION-EAST" "STORE-YUHUAN"
    manager_user_id TEXT    REFERENCES users(user_id),
    level           INTEGER NOT NULL,          -- 层级深度(0=总部, 1=大区, 2=区域, 3=门店)
    sort_order      INTEGER NOT NULL DEFAULT 0,
    config          TEXT,                       -- JSON 扩展配置
    is_active       BOOLEAN NOT NULL DEFAULT TRUE,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_org_parent ON org_structure(parent_id);
CREATE INDEX IF NOT EXISTS idx_org_type ON org_structure(node_type);
CREATE INDEX IF NOT EXISTS idx_org_level ON org_structure(level);
CREATE INDEX IF NOT EXISTS idx_org_code ON org_structure(code);
```

#### 表 18: audit_logs（操作审计）

> **对应功能**：全局操作审计、合规追溯

```sql
CREATE TABLE IF NOT EXISTS audit_logs (
    log_id          BIGSERIAL PRIMARY KEY,
    user_id         TEXT    REFERENCES users(user_id),
    action          TEXT    NOT NULL,          -- create / update / delete / login / export / ...
    target_type     TEXT    NOT NULL,          -- 目标资源类型: order / member / sop_template / ...
    target_id       TEXT,                       -- 目标资源ID
    old_value       TEXT,                       -- JSON 变更前值(仅update)
    new_value       TEXT,                       -- JSON 变更后值(仅update)
    ip_address      INET,
    user_agent      TEXT,
    result          TEXT    NOT NULL DEFAULT 'success',  -- success / failure / denied
    error_message   TEXT,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_audit_user ON audit_logs(user_id);
CREATE INDEX IF NOT EXISTS idx_audit_action ON audit_logs(action);
CREATE INDEX IF NOT EXISTS idx_audit_target ON audit_logs(target_type, target_id);
CREATE INDEX IF NOT EXISTS idx_audit_date ON audit_logs(created_at DESC);
CREATE INDEX IF NOT EXISTS idx_audit_result ON audit_logs(result);
```

#### 表 19: alerts（告警记录）

> **对应功能**：AlertGateway 告警持久化、告警确认与追溯

```sql
CREATE TABLE IF NOT EXISTS alerts (
    alert_id        TEXT PRIMARY KEY,          -- UUID v4
    store_id        TEXT    NOT NULL,
    alert_type      TEXT    NOT NULL,          -- sop_violation / temp_exceeded / stockout / device_offline / ...
    level           TEXT    NOT NULL,          -- critical / warn / info
    source          TEXT    NOT NULL,          -- vision / iot / system / data_engine
    summary         TEXT    NOT NULL,          -- 告警摘要
    detail          TEXT,                       -- JSON 详细信息
    status          TEXT    NOT NULL DEFAULT 'open',  -- open / acknowledged / resolved / suppressed
    acknowledged_by TEXT    REFERENCES users(user_id),
    acknowledged_at TIMESTAMPTZ,
    resolved_by     TEXT    REFERENCES users(user_id),
    resolved_at     TIMESTAMPTPTZ,
    push_channels   TEXT,                       -- JSON: [{channel, sent_at, delivered, read_at}]
    suppress_until  TIMESTAMPTZ,               -- 抑制截止时间(防告警风暴)
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_alerts_store ON alerts(store_id);
CREATE INDEX IF NOT EXISTS idx_alerts_type ON alerts(alert_type);
CREATE INDEX IF NOT EXISTS idx_alerts_level ON alerts(level);
CREATE INDEX IF NOT EXISTS idx_alerts_status ON alerts(status);
CREATE INDEX IF NOT EXISTS idx_alerts_created ON alerts(created_at DESC);
CREATE INDEX IF NOT EXISTS idx_alerts_source ON alerts(source);
```

### 2.3 索引策略

| 索引类型 | 用途 | 示例 |
|----------|------|------|
| **主键索引** | 唯一标识 | 所有表的 `id` / 复合唯一键 |
| **门店索引** | 按店查询（最常用） | `(store_id, ...)` |
| **时间索引** | 时间范围查询 | `(created_at DESC)` / `(business_date)` |
| **状态索引** | 按状态过滤 | `(status)` / `(level)` |
| **外键索引** | 关联查询 | `(batch_id)` / `(supplier_name)` |
| **部分索引** | 条件查询优化 | `WHERE status = 'pending'` |

### 2.4 数据生命周期

| 数据类别 | 热数据 | 温数据 | 冷数据 | 归档策略 |
|----------|:------:|:------:|:------:|----------|
| events | 7 天 | 30 天 | 90 天 | 90 天后归档至 S3/OSS |
| sales_daily | 365 天 | 2 年 | 永久 | 按年分区 |
| inventory_ledger | 90 天 | 1 年 | 3 年 | 3 年后聚合 |
| forecasts | 30 天 | 90 天 | 1 年 | 1 年后删除 |
| orders | 90 天 | 1 年 | 2 年 | 2 年后归档 |
| tasks | 90 天 | 180 天 | 1 年 | 完成后 1 年归档 |
| alerts | 30 天 | 90 天 | 180 天 | 180 天后删除 |
| device_registry | — | 永久 | — | 不归档 |
| users | — | 永久 | — | 不归档(含敏感信息需加密) |
| sop_templates | 180 天 | 2 年 | 永久 | 历史版本保留 |
| sop_violations | 90 天 | 1 年 | 3 年 | 合规审计保留 |
| knowledge_base | — | 永久 | — | 不归档(软删除) |
| members | — | 永久 | — | 不归档(脱敏后可归档) |
| points_transactions | 365 天 | 2 年 | 7 年 | 财务合规要求 |
| org_structure | — | 永久 | — | 不归档 |
| audit_logs | 90 天 | 1 年 | 3 年 | 合规+安全审计 |
| alerts | 30 天 | 90 天 | 1 年 | 1 年后聚合统计 |

---

## §3 RESTful API 规范

### 3.1 API 设计原则

1. **RESTful 风格**：资源名词 + HTTP 动词
2. **版本化 URL**：`/api/v1/...` （旧版无前缀，标记 deprecated）
3. **统一响应格式**：所有端点返回 `{ code, data, message }`
4. **分页**：列表接口支持 `?page=1&size=20`
5. **字段选择**：支持 `?fields=id,name,status`
6. **排序**：支持 `?sort=-created_at`（`-` 降序）

### 3.2 认证与授权

#### 认证方式

| 方式 | Header | 适用场景 | 优先级 |
|------|--------|----------|:------:|
| **JWT Bearer** | `Authorization: Bearer <token>` | 用户登录后的操作 | P0 |
| **API Key** | `X-Api-Key: <key>` | 边缘设备/系统集成 | P1 |

#### JWT Token 结构

```json
{
    "sub": "zhangdian",           // 用户ID
    "role": "店长",                // 角色
    "store_id": "store_yuhuan",   // 门店权限
    "exp": 1722240000,             // 过期时间戳
    "iat": 1722153600              // 签发时间
}
```

#### RBAC 角色-权限矩阵

| 角色 | 数据范围 | 写权限 | 可管理 | 读全部门店 |
|------|----------|:------:|:------:|:----------:|
| 店长 | 门店 | own_or_all | ❌ | ❌ |
| 前厅领班 | 门店 | own_or_all | ❌ | ❌ |
| 厨师长 | 门店 | own_or_all | ❌ | ❌ |
| 收货员 | 门店 | own | ❌ | ❌ |
| 班组长 | 门店 | own_or_all | ❌ | ❌ |
| 区域督导 | 区域 | all | ❌ | ✅ |
| 大区运营 | 大区 | all | ❌ | ✅ |
| 总部 PMO | 全国 | admin_write | ✅ | ✅ |
| 总部 IT | 全国 | admin_write | ✅ | ✅ |
| 营销运营 | 全国 | none | ❌ | ✅ |
| 财务审计 | 全国 | none | ❌ | ✅ |
| 集团决策者 | 全国 | none | ❌ | ✅ |

### 3.3 核心 API 端点清单

#### 3.3.1 认证

| Method | Path | 说明 | Auth |
|--------|------|------|:----:|
| POST | `/auth/token` | 用户登录（用户名+密码→JWT） | ❌ |
| GET | `/v1/auth/me` | 当前用户信息 | ✅ |

**POST /auth/token 请求**:
```json
{
    "username": "zhangdian",
    "password": "demo"
}
```

**响应**:
```json
{
    "code": 0,
    "data": {
        "access_token": "eyJhbGciOiJIUzI1NiIs...",
        "token_type": "bearer",
        "expires_in": 86400,
        "user": {
            "username": "zhangdian",
            "role": "店长",
            "name": "张店长",
            "store_id": "store_yuhuan"
        }
    },
    "message": "ok"
}
```

#### 3.3.2 设备管理

| Method | Path | 说明 | Auth |
|--------|------|------|:----:|
| GET | `/api/v1/devices` | 设备列表 | ✅ |
| POST | `/api/v1/devices/register` | 设备注册 | API Key |
| GET | `/api/v1/devices/{device_id}` | 设备详情 | ✅ |
| PUT | `/api/v1/devices/{device_id}/config` | 更新设备配置 | API Key |
| POST | `/api/v1/devices/{device_id}/heartbeat` | 设备心跳 | API Key |
| GET | `/api/v1/devices/{device_id}/pull-config` | 拉取最新配置 | API Key |

**POST /api/v1/devices/register 请求**:
```json
{
    "device_id": "jetson-yuhuan-01",
    "device_type": "jetson",
    "model": "Jetson Orin Nano",
    "firmware_ver": "1.2.3",
    "capabilities": ["vision", "vlm", "iot_bridge"],
    "store_id": "store_yuhuan"
}
```

**POST /api/v1/devices/{device_id}/heartbeat 请求**:
```json
{
    "timestamp": "2026-07-29T13:00:00Z",
    "uptime_seconds": 86400,
    "cpu_usage": 0.45,
    "memory_usage_mb": 2048,
    "gpu_usage": 0.62,
    "gpu_memory_mb": 3500,
    "temperature_c": 52.0,
    "disk_usage_pct": 67.0,
    "connected_cameras": 4,
    "inference_stats": {
        "total_inferences": 12580,
        "avg_latency_ms": 38.5,
        "success_rate": 0.998
    },
    "queue_depth": 0,
    "last_error": null
}
```

#### 3.3.3 事件摄入

| Method | Path | 说明 | Auth |
|--------|------|------|:----:|
| POST | `/api/v1/ingest` | 通用事件摄入 | API Key |
| POST | `/v1/iot/readings` | IoT 批量读数 | API Key |
| POST | `/v1/iot/readings/batch` | IoT 大批量读数 | API Key |

**POST /api/v1/ingest 请求** (视觉事件):
```json
{
    "event_type": "vision_detection",
    "source": "vision",
    "level": "info",
    "store_id": "store_yuhuan",
    "zone": "front",
    "camera_id": "cam_front_01",
    "timestamp": "2026-07-29T13:00:00Z",
    "image_ref": "/static/images/store_yuhuan/front_20260729_130000.jpg",
    "payload": {
        "detections": [
            {"class": "dish", "confidence": 0.92, "bbox": [100,200,300,350]},
            {"class": "person", "confidence": 0.88, "bbox": [400,300,550,700]}
        ],
        "table_states": [
            {"table_id": "T01", "state": "dining", "confidence": 0.95}
        ],
        "inference_ms": 38.5
    }
}
```

**POST /v1/iot/readings 请求** (IoT 事件):
```json
{
    "readings": [
        {
            "sensor_id": "temp_freezer_01",
            "type": "temperature",
            "value": -15.5,
            "unit": "°C",
            "timestamp": "2026-07-29T13:05:00Z",
            "location": "warehouse/freezer_a"
        },
        {
            "sensor_id": "weight_receiving_01",
            "type": "weight",
            "value": 50.2,
            "unit": "kg",
            "timestamp": "2026-07-29T13:06:00Z",
            "location": "receiving/scale_01"
        }
    ]
}
```

#### 3.3.4 收货质检

| Method | Path | 说明 | Auth |
|--------|------|------|:----:|
| POST | `/api/v1/receiving/checkin` | 收货登记 | ✅ |
| GET | `/api/v1/receiving/checkins` | 收货列表 | ✅ |
| POST | `/api/v1/receiving/submit` | 提交质检结果 | ✅ |
| POST | `/api/v1/receiving/quality-tap` | VLM 品质打分 | ✅ |
| GET | `/api/v1/receiving/batches` | 批次列表 | ✅ |
| GET | `/api/v1/receiving/supplier-stats` | 供应商统计 | ✅ |
| GET | `/v1/audit/signatures` | 签名审计 | ✅ |

**POST /api/v1/receiving/submit 请求**:
```json
{
    "batch_id": "RCV-20260729-001",
    "store_id": "store_yuhuan",
    "supplier_name": "杭州冻品供应链",
    "items": [
        {
            "sku": "冷冻牛肉卷",
            "po_weight_kg": 50.0,
            "actual_weight_kg": 49.2,
            "variance_pct": -1.6,
            "unit_price": 65.0,
            "vlm_grade": "A",
            "temp_c": -14.5,
            "batch_no": "B20260729001",
            "expiry_date": "2027-01-29"
        }
    ],
    "notes": "外包装完好，温度正常",
    "operator": "赵收货"
}
```

#### 3.3.5 订货建议

| Method | Path | 说明 | Auth |
|--------|------|------|:----:|
| GET | `/orders/{store_id}/suggestions` | 订货建议列表 | ✅ |
| POST | `/orders/suggestions/{id}/approve` | 审批通过 | ✅ |
| POST | `/orders/suggestions/{id}/reject` | 审批拒绝 | ✅ |
| POST | `/orders/batch-approve` | 批量审批 | ✅ |

**GET /orders/{store_id}/suggestions 响应**:
```json
{
    "code": 0,
    "data": {
        "store_id": "store_yuhuan",
        "generated_at": "2026-07-29T01:30:00Z",
        "items": [
            {
                "id": 101,
                "sku": "冷冻牛肉卷",
                "suggested_qty": 45.0,
                "unit": "kg",
                "current_stock": 12.5,
                "safety_stock": 15.0,
                "forecast_demand": 52.0,
                "lead_time_days": 1,
                "supplier": "杭州冻品供应链",
                "urgency": "normal",
                "reason": "日常补货，库存低于安全线",
                "status": "pending"
            }
        ],
        "summary": {
            "total_items": 15,
            "urgent_count": 2,
            "estimated_total": 3250.00
        }
    }
}
```

#### 3.3.6 销量预测

| Method | Path | 说明 | Auth |
|--------|------|------|:----:|
| GET | `/forecast/{store_id}` | 预测概览 | ✅ |
| POST | `/forecast/{store_id}/generate` | 触发预测生成 | ✅ |
| GET | `/forecast/{store_id}/{sku}` | 单 SKU 预测详情 | ✅ |

**GET /forecast/{store_id}/{sku} 响应**:
```json
{
    "code": 0,
    "data": {
        "store_id": "store_yuhuan",
        "sku": "毛肚",
        "forecasts": [
            {
                "date": "2026-07-30",
                "predicted_qty": 135.0,
                "confidence": 0.87,
                "lower_bound": 110.0,
                "upper_bound": 160.0,
                "model_version": "L2-sarima"
            }
        ],
        "history_7d": [
            {"date": "2026-07-22", "actual": 128.0},
            {"date": "2026-07-23", "actual": 142.0}
        ],
        "trend": "rising",
        "mape_30d": 10.6
    }
}
```

#### 3.3.7 库存管理

| Method | Path | 说明 | Auth |
|--------|------|------|:----:|
| GET | `/inventory/{store_id}` | 库存总览 | ✅ |
| GET | `/inventory/{store_id}/{sku}` | 单 SKU 详情 | ✅ |
| GET | `/inventory/{store_id}/movements` | 变动流水 | ✅ |
| POST | `/inventory/{store_id}/adjust` | 库存调整 | ✅ |
| GET | `/inventory/{store_id}/alerts` | 库存预警 | ✅ |

#### 3.3.8 任务管理

| Method | Path | 说明 | Auth |
|--------|------|------|:----:|
| GET | `/v1/tasks` | 任务列表 | ✅ |
| POST | `/v1/tasks` | 创建任务 | ✅ |
| GET | `/v1/tasks/{task_id}` | 任务详情 | ✅ |
| POST | `/v1/tasks/{task_id}/{action}` | 任务动作 | ✅ |
| POST | `/v1/tasks/ingest` | 系统自动创建任务 | System |

**任务动作 (action)**: `ack` | `start` | `complete` | `reopen` | `cancel` | `reassign` | `verify`

#### 3.3.9 告警管理

| Method | Path | 说明 | Auth |
|--------|------|------|:----:|
| GET | `/alerts/routes` | Webhook 状态 | ✅ |
| POST | `/alerts/test-push` | 测试推送 | ✅ |
| GET | `/alerts/push-log` | 推送日志 | ✅ |
| POST | `/alerts/ack` | 确认告警 | ✅ |
| GET | `/alerts/acks` | 确认审计 | ✅ |

#### 3.3.10 VLM 审核

| Method | Path | 说明 | Auth |
|--------|------|------|:----:|
| POST | `/api/v1/vlm/waste-estimate` | 废料估算 | ✅ |
| GET | `/api/v1/vlm/images/{event_id}` | 查看图片 | ✅ |

### 3.4 请求/响应格式

#### 统一成功响应

```json
{
    "code": 0,
    "data": { ... },
    "message": "ok"
}
```

#### 统一错误响应

```json
{
    "code": 40001,
    "data": null,
    "message": "参数校验失败: store_id 不能为空",
    "details": [
        {
            "field": "store_id",
            "message": "This field is required"
        }
    ]
}
```

#### 分页响应

```json
{
    "code": 0,
    "data": {
        "items": [ ... ],
        "total": 150,
        "page": 1,
        "size": 20,
        "pages": 8
    },
    "message": "ok"
}
```

### 3.5 错误码体系

| 错误码 | HTTP 状态码 | 说明 | 处理建议 |
|--------|:-----------:|------|----------|
| **0** | 200 | 成功 | — |
| **40001** | 400 | 参数校验失败 | 检查请求体 |
| **40002** | 400 | 参数格式错误 | 检查字段类型 |
| **40003** | 400 | 必填字段缺失 | 补全必填字段 |
| **40101** | 401 | 未认证 | 携带有效 Token |
| **40102** | 401 | Token 过期 | 重新登录 |
| **40103** | 401 | API Key 无效 | 检查 Key 配置 |
| **40301** | 403 | 权限不足 | 联系管理员 |
| **40302** | 403 | 门店越权 | 检查 store_id 权限 |
| **40303** | 403 | 操作不允许 | 检查角色权限 |
| **40401** | 404 | 资源不存在 | 检查 URL/ID |
| **40901** | 409 | 资源冲突 | 检查唯一约束 |
| **42201** | 422 | 业务规则违反 | 检查业务逻辑 |
| **42901** | 429 | 请求过于频繁 | 降低频率 |
| **50001** | 500 | 服务器内部错误 | 联系运维 |
| **50002** | 503 | 服务暂不可用 | 稍后重试 |
| **50301** | 503 | 数据库连接失败 | 检查数据库状态 |
| **50302** | 503 | 下游服务不可用 | 检查依赖服务 |

---

## §4 事件驱动协议

### 4.1 事件格式规范

所有事件遵循统一的 `OpsEvent` 结构（见 `common/schemas.py`）：

```python
@dataclass
class OpsEvent:
    event_type: str           # 事件类型标识
    source: str               # vision / iot / pos / system
    level: str                # critical / warn / info
    store_id: str             # 门店 ID
    message: str              # 人类可读描述
    event_id: str = uuid4()   # 全局唯一 ID
    timestamp: str = utc_now() # ISO-8601 UTC
    zone: str = ""            # front / kitchen / warehouse
    table_id: str = ""        # 桌号 (前厅场景)
    confidence: float = 1.0   # 置信度 0~1
    metadata: dict = field(default_factory=dict)  # 扩展元数据
```

### 4.2 事件类型分类

#### 视觉 AI 事件 (`source=vision`)

| event_type | level | 触发条件 | payload 关键字段 |
|------------|:-----:|----------|-----------------|
| `vision_detection` | info | 定期帧检测结果 | detections[], table_states[] |
| `table_need_clean` | info | 桌面需清理 | table_id, leftover_rate |
| `customer_leftover` | warn | 顾客剩余率高 | table_id, dish_count, estimated_waste_kg |
| `kitchen_waste` | warn | 后厨废料检测 | items[{sku, count, weight_kg}] |
| `kitchen_no_hat` | warn | 未戴厨师帽 | staff_id, zone |
| `kitchen_no_mask` | warn | 未戴口罩 | staff_id, zone |
| `kitchen_smoke` | critical | 后厨吸烟 | staff_id, zone, image_ref |

#### IoT 传感器事件 (`source=iot`)

| event_type | level | 触发条件 | payload 关键字段 |
|------------|:-----:|----------|-----------------|
| `iot_temp_abnormal` | warn | 温度超标 | sensor_id, value, threshold |
| `iot_weight_short` | warn | 称重短重 | sensor_id, variance_pct, po_weight |
| `iot_door_open_timeout` | warn | 门开启超时 | sensor_id, duration_sec |
| `iot_rfid_missing` | critical | RFID 缺扫 | expected_ids, missing_ids |
| `iot_fefo_violation` | warn | FEFO 违规 | sku, batch_id, reason |
| `iot_thaw_overtime` | warn | 解冻超时 | sku, started_at, duration_min |

#### POS 事件 (`source=pos`)

| event_type | level | 触发条件 | payload 关键字段 |
|------------|:-----:|----------|-----------------|
| `pos_sync` | info | POS 数据同步 | daily_revenue, turnover_rate, top_dishes[] |
| `pos_anomaly` | warn | 异常波动 | metric, value, baseline, deviation_pct |

#### 系统事件 (`source=system`)

| event_type | level | 触发条件 | payload 关键字段 |
|------------|:-----:|----------|-----------------|
| `device_online` | info | 设备上线 | device_id, type |
| `device_offline` | warn | 设备离线 | device_id, last_seen |
| `task_created` | info | 任务创建 | task_id, type, assignee |
| `task_overdue` | warn | 任务逾期 | task_id, assignee, due_at |
| `sop_violation` | warn | SOP 违规 | sop_id, sop_name, category |
| `forecast_generated` | info | 预测完成 | store_id, model_version, item_count |
| `order_approved` | info | 订单审批通过 | order_id, approver |

### 4.3 事件路由规则

Event Hub 内部路由矩阵：

| 事件源 | → 消费者 | 处理动作 |
|--------|----------|----------|
| vision_detection | TableStateTracker | 更新桌态 |
| vision_detection | WasteEstimator | 废料估算 |
| iot_temp_abnormal | ColdChainMonitor | 冷链告警 |
| iot_weight_short | LossRiskScorer | 损耗风险评分 |
| receiving_submit | SupplierScorecard | 供应商评分更新 |
| pos_sync | SalesIngestor | 日销量入库 |
| * (全部) | AlertGateway | 按 level 路由告警 |
| * (全部) | PersistHook | 持久化到 DB |
| * (全部) | DataEngineSubscriber | 数据引擎消费(可选) |

### 4.4 边缘→平台上报协议

**协议**: HTTPS POST (JSON)
**Content-Type**: `application/json`
**认证**: `X-Api-Key` Header

**上报流程**:
```
边缘采集 → 本地预处理 → EdgeHubClient.post()
    ├── 网络正常 → HTTP POST → Event Hub → 200 OK ✅
    └── 网络异常 → SQLite 入队 → 后台 flush → 恢复后重发 ✅
```

**批量上报优化** (边缘侧):
- 视觉事件: 每 10 秒批量聚合（最多 5 帧/批）
- IoT 读数: 每 30 秒批量上报（最多 50 条/批）
- 紧急事件 (critical): 立即上报，不等待批量

### 4.5 离线队列与重试

**离线队列实现**: `common/hub_client.py` → SQLite `pending` 表

**重试配置**:

| 参数 | 值 | 说明 |
|------|:---:|------|
| 最大队列深度 | 1000 条 | 超出后丢弃最旧 |
| Flush 间隔 | 30 秒 | 后台线程定时尝试 |
| 单次 Flush 上限 | 50 条 | 防止风暴 |
| HTTP 超时 | 5 秒 | 单次请求超时 |
| 最大重试次数 | 3 次 | 每条消息 |
| 重试退避 | 指数退避 | 1s → 2s → 4s |
| 过期清理 | 24 小时 | 超时未发送则删除 |

---

## §5 边缘-平台通信协议

### 5.1 心跳协议

**目的**: 监控边缘设备在线状态和健康指标

**端点**: `POST /api/v1/devices/{device_id}/heartbeat`
**频率**: 每 60 秒

**请求格式** (见 3.3.2 节完整示例):
```json
{
    "timestamp": "2026-07-29T13:00:00Z",
    "uptime_seconds": 86400,
    "cpu_usage": 0.45,
    "memory_usage_mb": 2048,
    "gpu_usage": 0.62,
    "gpu_memory_mb": 3500,
    "temperature_c": 52.0,
    "disk_usage_pct": 67.0,
    "connected_cameras": 4,
    "inference_stats": {
        "total_inferences": 12580,
        "avg_latency_ms": 38.5,
        "success_rate": 0.998
    },
    "queue_depth": 0,
    "last_error": null
}
```

**服务端判定规则**:

| 条件 | 判定 | 动作 |
|------|:----:|------|
| 最后心跳 < 2 min | online | 正常 |
| 最后心跳 2~5 min | offline | 标记离线，发送告警 |
| 最后心跳 > 5 min | offline | 升级告警到区域督导 |
| GPU 温度 > 75°C | warning | 温度告警 |
| 队列深度 > 100 | warning | 网络拥堵告警 |
| success_rate < 95% | warning | 推理异常告警 |

### 5.2 配置下发协议

**目的**: 平台向边缘设备推送配置更新（无需重启）

**端点**: `GET /api/v1/devices/{device_id}/pull-config`
**触发**: 边缘启动时 + 每 5 分钟轮询 + 平台主动通知(WebSocket 未来)

**响应格式**:
```json
{
    "code": 0,
    "data": {
        "config_version": "20260729.001",
        "updated_at": "2026-07-29T12:00:00Z",
        "camera_configs": [
            {
                "camera_id": "cam_front_01",
                "enabled": true,
                "stream_mode": "rtsp",
                "stream_url": "rtsp://192.168.2.10:554/stream1",
                "roi_tables": [...],
                "inference_zones": ["front"],
                "capture_interval_sec": 10
            }
        ],
        "inference_params": {
            "yolo_confidence_threshold": 0.5,
            "yolo_nms_threshold": 0.45,
            "strategy": "plan_b",
            "max_concurrent_streams": 4
        },
        "iot_config": {
            "mqtt_broker": "mqtt://localhost:1883",
            "topics_file": "mqtt_topics.json",
            "publish_interval_sec": 30
        },
        "alert_rules": {
            "cold_chain_warn_temp": -12.0,
            "cold_chain_critical_temp": -8.0,
            "weight_shortage_warn_pct": -3.0,
            "door_open_timeout_sec": 300
        },
        "hub_url": "http://192.168.2.85:8088",
        "store_id": "store_yuhuan"
    }
}
```

**边缘侧处理**:
1. 对比 `config_version`，无变化则跳过
2. 有变化则逐项应用（热更新，不重启推理进程）
3. 应用完成后回复确认
4. 应用失败则回滚到上一版

### 5.3 OTA 升级协议

**目的**: 远程升级边缘设备固件/模型/代码

**流程**:
```
平台发布新版本 → 边缘轮询发现新版本 → 下载包 → 校验 → 备份 → 安装 → 重启 → 上报结果
```

**端点** (规划中):

| Method | Path | 说明 |
|--------|------|------|
| GET | `/api/v1/devices/{device_id}/ota/check` | 检查新版本 |
| POST | `/api/v1/devices/{device_id}/ota/download` | 通知下载 |
| POST | `/api/v1/devices/{device_id}/ota/confirm` | 确认安装结果 |

**OTA 包结构**:
```
hotpot-edge-v1.2.3.tar.gz
├── manifest.json          # 版本清单+校验和
├── bin/
│   └── edge_agent         # 主程序二进制
├── models/
│   ├── yolo_best.pt       # YOLO 模型
│   └── clip_model.bin     # CLIP 模型
├── config/
│   └── default.json       # 默认配置
└── scripts/
    ├── post_install.sh    # 安装后脚本
    └── rollback.sh        # 回滚脚本
```

---

## §6 部署配置清单

### 6.1 环境变量矩阵

#### Event Hub (平台)

| 变量名 | 必填 | 默认值 | 说明 | 敏感 |
|--------|:----:|:------:|------|:----:|
| `HOTPOT_ENV` | 否 | `dev` | 部署环境 | ❌ |
| `HOTPOT_AUTH_MODE` | 否 | `demo` | demo/strict | ❌ |
| `HOTPOT_DATABASE_URL` | 否 | 空(SQLite) | PostgreSQL 连接串 | ✅ |
| `HOTPOT_DB` | 否 | `demo/data/hub.db` | SQLite 路径 | ❌ |
| `HOTPOT_JWT_SECRET` | **是** | `hotpot-dev-secret-change-in-prod` | JWT 签名密钥 | ✅ |
| `HOTPOT_JWT_HOURS` | 否 | `24` | Token 有效期(小时) | ❌ |
| `HOTPOT_CORS_ORIGINS` | 否 | `*`(dev) | CORS 白名单 | ❌ |
| `HOTPOT_EDGE_API_KEYS` | 否 | 内置默认 | 边缘 API Key 映射 | ✅ |
| `HOTPOT_WECHAT_WEBHOOK_*` | 否 | 空 | 企业微信 Webhook | ✅ |
| `HOTPOT_PUSH_WARN` | 否 | `0` | 是否推送 warn 级别 | ❌ |
| `HOTPOT_STORE_TZ` | 否 | `Asia/Shanghai` | 门店时区 | ❌ |
| `HOTPOT_DAILY_REPORT_HOUR` | 否 | `22` | 日报生成时间(时) | ❌ |
| `HOTPOT_DAILY_REPORT_MINUTE` | 否 | `0` | 日报生成时间(分) | ❌ |

#### Edge Agent (边缘)

| 变量名 | 必填 | 默认值 | 说明 | 敏感 |
|--------|:----:|:------:|------|:----:|
| `HOTPOT_MODE` | 是 | `agent` | 运行模式 | ❌ |
| `HOTPOT_HUB_URL` | **是** | — | Event Hub 地址 | ❌ |
| `HOTPOT_STORE_ID` | **是** | — | 门店 ID | ❌ |
| `HOTPOT_ACCOUNT_ID` | 否 | `default` | 账户 ID | ❌ |
| `HOTPOT_DEVICE_ID` | **是** | — | 设备 ID | ❌ |
| `HOTPOT_API_KEY` | **是** | — | API Key | ✅ |
| `NVIDIA_VISIBLE_DEVICES` | 否 | `all` | 可见 GPU | ❌ |
| `LLAMA_NGL` (Jetson) | 否 | `999` | LLM GPU 分层数 | ❌ |

### 6.2 Docker Compose 模板

#### Cloud (平台) - `deploy/cloud/docker-compose.yml`

```yaml
services:
  hub:
    image: python:3.11-slim-bookworm
    working_dir: /app
    command: >
      bash -c "apt-get update -qq && apt-get install -y -qq libgl1 libglib2.0-0 libpq-dev gcc >/dev/null
      && pip install -q -r requirements.txt
      && python hotpot_platform/cloud/event_hub/server.py
        --host 0.0.0.0 --port 8088
        --seed-dir demo/data/stores"
    ports: ["${HUB_PORT:-8088}:8088"]
    environment:
      HOTPOT_ENV: ${HOTPOT_ENV:-dev}
      HOTPOT_AUTH_MODE: ${HOTPOT_AUTH_MODE:-demo}
      HOTPOT_DATABASE_URL: ${HOTPOT_DATABASE_URL:-}
      HOTPOT_JWT_SECRET: ${HOTPOT_JWT_SECRET:-}
      HOTPOT_CORS_ORIGINS: ${HOTPOT_CORS_ORIGINS:-}
      HOTPOT_EDGE_API_KEYS: ${HOTPOT_EDGE_API_KEYS:-}
      HOTPOT_WECHAT_WEBHOOK: ${HOTPOT_WECHAT_WEBHOOK:-}
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8088/health')"]
      interval: 15s
      timeout: 5s
      retries: 3

  dashboard:
    image: python:3.11-slim-bookworm
    working_dir: /app
    command: python hotpot_platform/dashboard/serve.py --host 0.0.0.0 --port 3000
    ports: ["${DASHBOARD_PORT:-3000}:3000"]
    depends_on:
      hub: { condition: service_healthy }

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${PG_USER:-hotpot}
      POSTGRES_PASSWORD: ${PG_PASSWORD:-hotpot_dev}
      POSTGRES_DB: ${PG_DB:-hotpot_ops}
    ports: ["${PG_PORT:-5432}:5432"]
    volumes: [hotpot_pg_data:/var/lib/postgresql/data]
    profiles: [postgres]

  mqtt:
    image: eclipse-mosquitto:2
    ports: ["${MQTT_PORT:-1883}:1883"]
    profiles: [iot]

volumes:
  hotpot_pg_data:
```

#### Edge (边缘) - `deploy/edge/docker-compose.yml`

```yaml
services:
  edge:
    build: .
    container_name: hotpot-edge
    restart: unless-stopped
    runtime: nvidia
    ports: ["${EDGE_PORT:-9100}:9100"]
    volumes:
      - /opt/hotpot-infer/models:/opt/hotpot-infer/models:ro
      - /opt/hotpot-infer/data:/opt/hotpot-infer/data
      - /tmp/ipc_frames:/tmp/ipc_frames
    environment:
      NVIDIA_VISIBLE_DEVICES: all
      HOTPOT_MODE: agent
      HOTPOT_HUB_URL: ${HOTPOT_HUB_URL:?err}
      HOTPOT_STORE_ID: ${HOTPOT_STORE_ID:?err}
      HOTPOT_DEVICE_ID: ${HOTPOT_DEVICE_ID:?err}
      HOTPOT_API_KEY: ${HOTPOT_API_KEY:?err}
    network_mode: host
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9100/health"]
      interval: 30s
      timeout: 10s
      retries: 3
```

### 6.3 密钥管理策略

| 密钥类型 | 存储位置 | 轮换策略 | 访问控制 |
|----------|----------|----------|----------|
| JWT Secret | 环境变量 / Vault | 每 90 天 | 仅 DevOps |
| Edge API Keys | 环境变量 / Vault | 泄露时立即更换 | 仅 DevOps |
| 企业微信 Webhook | 环境变量 | 变更时更新 | 仅管理员 |
| 数据库密码 | 环境变量 / Vault | 每 180 天 | 仅 DevOps |
| MQTT Password | 配置文件 (加密) | 泄露时更换 | 仅 DevOps |

**生产环境要求**:
- ❌ 禁止在代码中硬编码任何密钥
- ❌ 禁止将密钥提交到 Git 仓库
- ✅ 使用 HashiCorp Vault 或云厂商 KMS
- ✅ 最小权限原则：每个 Edge 设备独立 API Key
- ✅ 审计日志：所有密钥访问记录

---

## §7 监控告警规则

### 7.1 核心指标定义

#### 平台层指标

| 指标名 | 类型 | 单位 | 说明 | 采集方式 |
|--------|------|------|------|----------|
| `http_requests_total` | Counter | 次 | HTTP 请求总数 | FastAPI middleware |
| `http_request_duration_ms` | Histogram | ms | 请求延迟分布 | FastAPI middleware |
| `http_errors_total` | Counter | 次 | HTTP 错误总数 | FastAPI middleware |
| `events_ingested_total` | Counter | 件 | 事件摄入总数 | EventStore.add_event |
| `event_processing_ms` | Histogram | ms | 事件处理延迟 | EventStore.add_event |
| `db_query_duration_ms` | Histogram | ms | 数据库查询延迟 | DB wrapper |
| `active_stores` | Gauge | 个 | 在线门店数 | heartbeat 判定 |
| `alert_sent_total` | Counter | 条 | 告警发送总数 | AlertGateway |
| `alert_push_fail_total` | Counter | 条 | 告警推送失败数 | AlertGateway |
| `jwt_tokens_issued` | Counter | 个 | JWT 签发总数 | auth_routes |
| `forecast_generation_ms` | Histogram | ms | 预测生成耗时 | SalesPredictor |

#### 边缘层指标

| 指标名 | 类型 | 单位 | 说明 | 采集方式 |
|--------|------|------|------|----------|
| `inference_total` | Counter | 帧 | 推理总帧数 | Vision Worker |
| `inference_latency_ms` | Histogram | ms | 推理延迟 | Vision Worker |
| `inference_success_rate` | Gauge | % | 推理成功率 | Vision Worker |
| `gpu_utilization` | Gauge | % | GPU 利用率 | nvidia-smi |
| `gpu_memory_used_mb` | Gauge | MB | GPU 显存占用 | nvidia-smi |
| `gpu_temperature_c` | Gauge | °C | GPU 温度 | nvidia-smi |
| `cpu_utilization` | Gauge | % | CPU 利用率 | psutil |
| `memory_usage_mb` | Gauge | MB | 内存占用 | psutil |
| `disk_usage_pct` | Gauge | % | 磁盘利用率 | shutil |
| `offline_queue_depth` | Gauge | 条 | 离线队列深度 | EdgeHubClient |
| `upload_success_rate` | Gauge | % | 上传成功率 | EdgeHubClient |
| `camera_connected` | Gauge | 个 | 在线摄像头数 | stream check |
| `mqtt_messages_rx` | Counter | 条 | MQTT 接收消息数 | MQTT callback |

### 7.2 告警阈值与分级

#### P0 — 立即处理（电话+短信+企微）

| 指标 | 条件 | 告警内容 | 通知对象 |
|------|------|----------|----------|
| `http_errors_total` | 5min 错误率 > 10% | 平台 HTTP 错误率过高 | DevOps |
| `db_query_duration_ms` | P99 > 5000ms | 数据库查询严重超时 | DevOps |
| `alert_push_fail_total` | 5min 失败 > 5 条 | 告警通道阻塞 | DevOps |
| `gpu_temperature_c` | > 80°C | 边缘 GPU 过热 | DevOps |
| `offline_queue_depth` | > 500 条 | 边缘离线队列积压 | DevOps |
| `store_offline` | 离线 > 10min | 门店完全离线 | 区域督导 |

#### P1 — 1 小时内处理（企微）

| 指标 | 条件 | 告警内容 | 通知对象 |
|------|------|----------|----------|
| `http_request_duration_ms` | P95 > 2000ms | API 响应缓慢 | DevOps |
| `event_processing_ms` | P95 > 1000ms | 事件处理积压 | DevOps |
| `inference_latency_ms` | P95 > 100ms | 边缘推理缓慢 | DevOps |
| `inference_success_rate` | < 95% | 边缘推理失败率偏高 | DevOps |
| `gpu_memory_used_mb` | > 90% 容量 | GPU 显存不足 | DevOps |
| `disk_usage_pct` | > 85% | 磁盘空间不足 | DevOps |
| `upload_success_rate` | < 90% | 边缘上传成功率低 | DevOps |
| `forecast_generation_ms` | > 60000ms | 预测生成超时 | DevOps |

#### P2 — 当日处理（企微汇总）

| 指标 | 条件 | 告警内容 | 通知对象 |
|------|------|----------|----------|
| `active_stores` | < 总门店数 × 80% | 部分门店离线 | 区域督导 |
| `camera_connected` | < 配置数 × 90% | 部分摄像头掉线 | 店长 |
| `jwt_tokens_issued` | 异常突增 | 可能存在暴力破解 | 安全团队 |
| `mqtt_messages_rx` | 5min 为 0 | MQTT 消息中断 | DevOps |

### 7.3 健康检查端点

| 端点 | 方法 | 返回内容 | 用途 |
|------|:----:|----------|------|
| `/health` | GET | `{"status":"ok","version":"..."}` | 存活探针 (liveness) |
| `/ready` | GET | `{"ready":true,"checks":{"db":true,...}}` | 就绪探针 (readiness) |
| `/metrics` | GET | Prometheus 格式指标 | 指标采集 |
| `/benchmark` | GET | 性能基准数据 | 压力测试验证 |
| `/accuracy` | GET | 模型准确度统计 | 模型监控 |

**边缘健康检查** (`edge:9100/health`):

```json
{
    "status": "ok",
    "device_id": "jetson-yuhuan-01",
    "store_id": "store_yuhuan",
    "uptime_seconds": 86400,
    "version": "1.2.3",
    "components": {
        "yolo_engine": "healthy",
        "clip_engine": "healthy",
        "mqtt_client": "connected",
        "hub_connection": "ok",
        "camera_01": "streaming",
        "camera_02": "streaming",
        "camera_03": "offline",
        "camera_04": "streaming"
    },
    "stats": {
        "inferences_last_hour": 3600,
        "avg_latency_ms": 38.5,
        "success_rate": 0.998,
        "queue_depth": 0,
        "events_sent_last_hour": 2850
    }
}
```

---

## §8 接口一致性矩阵

### 逻辑架构 ↔ 开发架构映射

| 逻辑模块 | 代码位置 | 进程/线程 | 端口 | 通信协议 |
|----------|----------|-----------|:----:|----------|
| 视觉推理引擎 | `edge/front_hall/inference/` | Edge Agent 进程内线程 | — | 内部函数调用 |
| YOLO 检测 | `edge/front_hall/inference/engines/yolo.py` | 子进程 (GPU) | — | 共享内存 |
| CLIP 理解 | `edge/front_hall/inference/engines/clip_client.py` | 子进程 (CPU/RAM) | — | stdin/stdout JSON |
| IoT Bridge | `edge/front_hall/iot/mqtt_bridge.py` | Edge Agent 进程内线程 | — | MQTT |
| Edge Hub Client | `common/hub_client.py` | Edge Agent 进程内线程 | — | HTTPS POST |
| Event Hub Core | `hotpot_platform/cloud/event_hub/hub_core.py` | Uvicorn 主线程 | 8088 | FastAPI |
| Alert Gateway | `hotpot_platform/cloud/alert_gateway/gateway.py` | Event Hub 进程内线程 | — | 企业微信 Webhook |
| 数据引擎 | `hotpot_platform/cloud/data_engine/` | 定时任务线程 / API 请求 | — | 内部调用 |
| ERP Connector | `hotpot_platform/cloud/data_engine/erp_connector.py` | API 请求时 | — | HTTP(S) |
| POS Bridge | `hotpot_platform/cloud/integrations/pos_bridge.py` | 定时任务线程 | — | HTTPS POST |
| Daily Scheduler | `hotpot_platform/cloud/event_hub/daily_scheduler.py` | 后台线程 | — | 内部定时器 |
| VLM Review | `hotpot_platform/cloud/vlm_review/` | 独立 Uvicorn 进程 | 8089 | FastAPI |
| Dashboard | `hotpot_platform/dashboard/` | 独立 Uvicorn 进程 | 3000 | FastAPI |

### 数据模型 ↔ Schema 映射

| Pydantic 模型 | 文件 | 对应 DB 表 | 字段对应关系 |
|---------------|:-----|:-----------|-------------|
| `SalesRecord` | `data_engine/models.py` | `sales_daily` | 1:1 字段映射 |
| `SalesForecast` | `data_engine/models.py` | `sales_forecast` | 1:1 字段映射 |
| `InventoryMovement` | `data_engine/models.py` | `inventory_ledger` | 1:1 字段映射 |
| `InventorySnapshot` | `data_engine/models.py` | `inventory_snapshot` | 1:1 字段映射 |
| `OrderSuggestion` | `data_engine/models.py` | `order_suggestion` | 1:1 字段映射 |
| `LossAnalysis` | `data_engine/models.py` | 聚合视图 | N04 计算 |
| `LossTrend` | `data_engine/models.py` | 聚合视图 | N04 计算 |
| `SupplierScorecard` | `data_engine/models.py` | `supplier_scorecard` | 1:1 字段映射 |
| `ErpSyncResult` | `data_engine/models.py` | 操作日志表 | N06 同步记录 |
| `OpsEvent` | `common/schemas.py` | `events` | 1:1 字段映射 |
| `TableState` | `common/schemas.py` | `store_snapshots` (kind=table) | 1:1 字段映射 |

### PRD 功能 ↔ 架构全映射（完整版 v1.5）

> **说明**：本矩阵以 **PRD v5.3n 为编号唯一源**，覆盖全部 **129 项功能**，含 API 端点、DB 表、代码位置、实现状态四维映射。
> ⚠️ **功能定义以PRD为准**：N19/N21/N22的定义已在v1.4修正对齐PRD，详见下方表格。
> **状态图例**：✅ 已实现(含Sprint0通过) | 🔧 代码基础 | 📐 规则骨架 | ⬜ 未来规划 | 🆕 本文档新增定义

#### K 系列 — 视觉前厅（70 项）

| ID | 功能名称 | API 端点 | DB 表 | 代码位置 | 状态 |
|:--:|----------|---------|-------|---------|:----:|
| K01 | 桌态识别 | `POST /api/v1/ingest` (vision) | events, store_snapshots | `edge/front_hall/inference/vision_worker.py` | 🔧 |
| K02 | 顾客人数统计 | `POST /api/v1/ingest` (vision) | events | `edge/front_hall/inference/vision_worker.py` | 🔧 |
| K03 | 剩余率估算 | `POST /api/v1/vlm/waste-estimate` | events | `hotpot_platform/cloud/vlm_review/` | 🔧 |
| K04 | 翻台率分析 | `GET /api/v1/turnover/rate` | sales_daily, events | `event_hub/routers/turnover.py` | 🔧 |
| K05 | 加汤提醒 | `POST /api/v1/ingest` (vision) | events, tasks | `edge/front_hall/inference/` | 📐 |
| K06 | 餐具识别 | `POST /api/v1/ingest` (vision) | events | `edge/front_hall/inference/engines/yolo.py` | ⬜ |
| K07 | 菜品识别 | `POST /api/v1/ingest` (vision) | events | `edge/front_hall/inference/engines/yolo.py` | ⬜ |
| K08 | 服务员行为识别 | `POST /api/v1/staff-behavior` | events, tasks | `event_hub/routers/staff_behavior.py` | 🔧 |
| K09 | 排队检测 | `POST /api/v1/ingest` (vision) | events | — | ⬜ |
| K10 | 等位时长统计 | `GET /api/v1/queue/stats` | events | — | ⬜ |
| K11 | 等位流失预警 | Event Hub → AlertGateway | alerts | `hotpot_platform/cloud/alert_gateway/gateway.py` | ⬜ |
| K12 | 顾客情绪识别 | `POST /api/v1/ingest` (vision+CLIP) | events | `edge/front_hall/inference/engines/clip_client.py` | ⬜ |
| K13 | 卫生检查(桌面) | SOPChecker.check_compliance() | sop_violations | `sop_engine/checker.py` | 🆕 |
| K14 | 卫生检查(地面) | SOPChecker.check_compliance() | sop_violations | `sop_engine/checker.py` | 🆕 |
| K15 | 卫生检查(餐具) | SOPChecker.check_compliance() | sop_violations | `sop_engine/checker.py` | 🆕 |
| K16 | 安全监控(烫伤风险) | Event Hub → AlertGateway | alerts | — | ⬜ |
| K17 | 安全监控(儿童安全) | Event Hub → AlertGateway | alerts | — | ⬜ |
| K18 | 能耗检测(灯光) | `POST /v1/iot/readings` | events | `event_hub/routers/iot.py` | ⬜ |
| K19 | 能耗检测(空调) | `POST /v1/iot/readings` | events | `event_hub/routers/iot.py` | ⬜ |
| K20-K36 | Phase 1 扩展视觉 | — | — | — | ⬜ |
| K37-K55 | 多店视觉扩展 | — | — | — | ⬜ |
| K56-K73 | Phase 2 开放平台 | — | — | — | ⬜ |

#### N 系列 — 数据引擎（27 项）

| ID | 功能名称 | API 端点 | DB 表 | 代码位置 | 状态 |
|:--:|----------|---------|-------|---------|:----:|
| N01 | 销量预测 | `GET /forecast/{store_id}/{sku}` | sales_forecast, sales_daily | `data_engine/sales_predictor.py` | ✅ |
| N02 | 订货建议 | `GET /orders/{store_id}/suggestions` | order_suggestion, inventory_snapshot | `data_engine/order_advisor.py` | 📐 |
| N03 | 库存管理 | `GET /inventory/{store_id}` | inventory_ledger, inventory_snapshot | `data_engine/` | 📐 |
| N04 | 损耗分析 | `GET /loss/{store_id}/rate` | inventory_ledger, sales_daily | `data_engine/loss_analyzer.py` | 📐 |
| N05 | 供应商评分 | `GET /suppliers/{name}/scorecard` | supplier_scorecard, receiving_batches | `data_engine/` | 📐 |
| N06 | ERP 对接 | `POST /erp/{store_id}/sync` | 操作日志 | `data_engine/erp_connector.py` | 🔧 |
| N07 | 价格波动追踪 | `GET /analytics/pricing/trend` | sales_daily | — | ⬜ |
| N08 | 成本结构分析 | `GET /analytics/cost/structure` | inventory_ledger, sales_daily | — | ⬜ |
| N09 | 毛利实时计算 | `GET /analytics/margin/realtime` | sales_daily, inventory_ledger | — | ⬜ |
| N10 | ABC 分类 | `GET /inventory/{store_id}/abc` | inventory_snapshot, sales_daily | — | ⬜ |
| N11 | DSR 日销报表 | `GET /reports/daily/{store_id}` | sales_daily | `event_hub/routers/daily_report.py` | 🔧 |
| N12 | 周报自动生成 | `GET /reports/weekly/{store_id}` | sales_daily, inventory_ledger | — | ⬜ |
| N13 | 业态对比分析 | `GET /analytics/benchmark` | sales_daily | — | ⬜ |
| N14 | 同比环比 | `GET /analytics/yoy-yoy` | sales_daily | — | ⬜ |
| N15 | 菜品贡献度 | `GET /analytics/dish/contribution` | sales_daily | — | ⬜ |
| N16 | 时段热力图 | `GET /analytics/hourly/heatmap` | sales_daily(hour_dist) | — | ⬜ |
| N17 | 预测回灌闭环 | `POST /forecast/{store_id}/generate` | sales_forecast | `data_engine/sales_predictor.py` | ✅ |
| N18 | 自动补货触发 | Event Hub → OrderAdvisor | order_suggestion | `data_engine/order_advisor.py` | ⬜ |
| N19 | **跨店库存调拨** | `POST /api/v1/inventory/cross-store/transfer` | inventory_ledger, stores | `data_engine/inventory_transfer.py` | ⬜ |
| N20 | 需求异常检测 | Event Hub → AlertGateway | alerts, sales_forecast | — | ⬜ |
| N21 | **库位管理** | `GET/POST /api/v1/inventory/{store_id}/locations` | inventory_snapshot, k36_locations | `data_engine/location_mgr.py` | ⬜ |
| N22 | **AI菜品推荐** | `GET /api/v1/recommendations/dishes/{store_id}` | sales_daily, menu_items | `data_engine/dish_recommender.py` | ⬜ |
| N23 | 人效分析 | `GET /analytics/labor/productivity` | sales_daily, events | — | ⬜ |
| N24 | 平效分析 | `GET /analytics/revenue-per-sqm` | sales_daily | — | ⬜ |
| N25 | 废料趋势 | `GET /api/v1/waste/trend` | events | `event_hub/routers/waste_trend.py` | 🔧 |
| N26 | RFID 批次关联 | RFIDTracker.track_batch() | inventory_ledger | `warehouse/rfid_tracker.py` | 🆕 |

#### S 系列 — 供应链（4 项）

| ID | 功能名称 | API 端点 | DB 表 | 代码位置 | 状态 |
|:--:|----------|---------|-------|---------|:----:|
| S01 | 供应商管理 | `POST /v1/suppliers` + CRUD | supplier_scorecard | — | ⬜ |
| S02 | 收货质检(VLM) | `POST /api/v1/receiving/submit` | receiving_batches, device_registry | `event_hub/routers/receiving.py` | 🔧 |
| S03 | 采购订单管理 | `POST /v1/purchase-orders` | inventory_ledger | — | ⬜ |
| S04 | 供应商协同 | `POST /v1/suppliers/{id}/collab` | supplier_scorecard | — | ⬜ |

#### A 系列 — AI 助理（5 项）

| ID | 功能名称 | API 端点 | DB 表 | 代码位置 | 状态 |
|:--:|----------|---------|-------|---------|:----:|
| A01 | 岗位AI助理(前厅) | `POST /v1/assistant/chat` | tasks, knowledge_base | — | ⬜ |
| A02 | 岗位AI助理(后厨) | `POST /v1/assistant/chat` | tasks, knowledge_base | — | ⬜ |
| A03 | 岗位AI助理(店长) | `POST /v1/assistant/chat` | tasks, knowledge_base, members | — | ⬜ |
| A04 | 任务自动分发 | Event Hub → TaskRouter | tasks | `event_hub/core/task_router.py` | 📐 |
| A05 | 执行结果回写 | `PATCH /v1/tasks/{id}` | tasks | `event_hub/routers/tasks.py` | 🔧 |

#### SC 系列 — SOP 合规（3 项）

| ID | 功能名称 | API 端点 | DB 表 | 代码位置 | 状态 |
|:--:|----------|---------|-------|---------|:----:|
| SC01 | SOP模板库管理 | CRUD `/v1/sop/templates` | sop_templates | `sop_engine/template_manager.py` | 🆕 |
| SC02 | 实时合规检查 | `POST /v1/sop/check` | sop_violations, sop_templates | `sop_engine/checker.py` | 🆕 |
| SC03 | 违规记录与追踪 | GET+PATCH `/v1/sop/violations` | sop_violations | `sop_engine/violation_tracker.py` | 🆕 |

#### KT 系列 — 知识库（2 项）

| ID | 功能名称 | API 端点 | DB 表 | 代码位置 | 状态 |
|:--:|----------|---------|-------|---------|:----:|
| KT01 | 菜品知识库 | POST `/v1/knowledge/query` (dish) | knowledge_base | `knowledge/retriever.py` | 🆕 |
| KT02 | 经营Know-how检索 | POST `/v1/knowledge/query` (operation) | knowledge_base | `knowledge/retriever.py` | 🆕 |

#### WH 系列 — 仓库 IoT（6 项）

| ID | 功能名称 | API 端点 | DB 表 | 代码位置 | 状态 |
|:--:|----------|---------|-------|---------|:----:|
| WH01 | RFID批次追踪 | `POST /v1/warehouse/rfid/track` | inventory_ledger | `warehouse/rfid_tracker.py` | 🆕 |
| WH02 | 温湿度监控 | MQTT ingest + GET `/v1/warehouse/iot/status` | events(device_registry扩展) | `warehouse/iot_monitor.py` | 🆕 |
| WH03 | FEFO先失效先出 | `GET /v1/warehouse/fevo/check` | inventory_snapshot | `warehouse/fefo_monitor.py` | 🆕 |
| WH04 | 库存预警 | Event Hook + GET `/v1/warehouse/alerts` | alerts, inventory_snapshot | `warehouse/inventory_alertor.py` | 🆕 |
| WH05 | 效期管理 | `GET /v1/warehouse/expiry/list` | inventory_snapshot | `warehouse/fefo_monitor.py` | 🆕 |
| WH06 | IoT设备管理 | CRUD `/v1/warehouse/iot/devices` | device_registry | `warehouse/iot_monitor.py` | 🆕 |

#### FH 系列 — 会员营销（4 项）

| ID | 功能名称 | API 端点 | DB 表 | 代码位置 | 状态 |
|:--:|----------|---------|-------|---------|:----:|
| FH01 | 会员视觉识别 | Edge internal → `POST /v1/members/visits` | members | `member/recognizer.py`(edge) + `service.py`(cloud) | 🆕 |
| FH02 | 积分系统 | CRUD `/v1/members/{id}/points` | members, points_transactions | `member/loyalty.py` | 🆕 |
| FH03 | 精准营销 | CRUD `/v1/marketing/campaigns` | members(knowledge_base引用) | `member/marketing.py` | 🆕 |
| FH04 | 会员画像 | `GET /v1/members/{id}/profile` | members, points_transactions | `member/service.py` | 🆕 |

#### H 系列 — 连锁管控（14 项）

| ID | 功能名称 | API 端点 | DB 表 | 代码位置 | 状态 |
|:--:|----------|---------|-------|---------|:----:|
| H01 | 组织架构管理 | CRUD `/v1/org/nodes` | org_structure, users | `event_hub/routers/org.py` | 🔧 |
| H02 | 多店数据看板 | `GET /v1/national/dashboard` | sales_daily(多店聚合) | — | ⬜ |
| H03 | 跨店对比分析 | `GET /v1/national/compare` | sales_daily(多店) | — | ⬜ |
| H04 | 统一菜品管理 | CRUD `/v1/national/menu` | — | — | ⬜ |
| H05 | 统一供应商目录 | CRUD `/v1/national/suppliers` | supplier_scorecard | — | ⬜ |
| H06 | 区域督导巡检 | `POST /v1/national/inspection` | sop_violations, audit_logs | — | ⬜ |
| H07 | 总部运营报表 | `GET /v1/national/reports` | sales_daily(全国聚合) | — | ⬜ |
| H08 | 权限继承(HRBAC) | JWT 签发时解析 | users, org_structure | `event_hub/auth/` | ⬜ |
| H09-H14 | Phase 2 连锁扩展 | — | — | — | ⬜ |

#### 映射统计汇总

| 维度 | 数值 | 说明 |
|------|:----:|------|
| **PRD 总功能数** | **129** | P0(27)+P1(42)+P2(56)+Phase2(4) |
| **有 API 端点** | **38** | 29.5% |
| **有 DB 表映射** | **52** | 40.3% |
| **有代码位置** | **24** | 18.6% |
| **✅ 已实现** | **2** | N01, N17 (Sprint 0 通过) |
| **🔧 代码基础** | **12** | 含边缘推理+API路由 |
| **📐 规则骨架** | **6** | 有接口定义但逻辑不完整 |
| **⬜ 未来规划** | **82** | 63.6% |
| **🆕 本文档新增** | **23** | §1.6-1.9 新增的接口定义 |

---

## 附录

### A. 术语表

| 术语 | 全称 | 说明 |
|------|------|------|
| Hub | Event Hub | 多租户事件中枢平台 |
| Edge | Edge Agent | 边缘推理盒子 (Jetson) |
| IPC | IP Camera | 网络摄像头 |
| VLM | Vision Language Model | 视觉语言大模型 |
| MAPE | Mean Absolute Percentage Error | 平均绝对百分比误差 |
| EOQ | Economic Order Quantity | 经济订货量 |
| FEFO | First Expired First Out | 先失效先出 |
| ROI | Region of Interest | 感兴趣区域 (画面中的检测框) |
| SLA | Service Level Agreement | 服务等级协议 |
| OTA | Over-The-Air | 远程空中升级 |
| RBAC | Role-Based Access Control | 基于角色的访问控制 |
| SOP | Standard Operating Procedure | 标准作业程序 |
| MQTT | Message Queuing Telemetry Transport | 物联网消息协议 |
| JWT | JSON Web Token | JSON 网络令牌 |

### B. 端口清单

| 端口 | 服务 | 协议 | 方向 | 说明 |
|------|------|:----:|:----:|------|
| 8088 | Event Hub | HTTP | 内部← | 平台 API 主入口 |
| 8089 | VLM Review | HTTP | 内部← | VLM 审核 API |
| 3000 | Dashboard | HTTP | 外部→ | 数字座舱前端 |
| 5432 | PostgreSQL | TCP | 内部 | 数据库 (prod) |
| 1883 | MQTT | TCP | 内部 | IoT 消息代理 |
| 9100 | Edge Agent | HTTP | 内部← | 边缘推理 API |
| 554 | RTSP | TCP | 内部← | 摄像头视频流 (未来) |

### C. 文件依赖关系

```
详细架构设计 v1.6 (本文档)
  ├── 系统架构总体设计 v2.1      (父文档, L1 架构) ★已同步
  ├── 数据引擎技术设计 v1.0       (N01-N06 算法细节)
  ├── PRD v5.3n 主线基线          (唯一权威, 129项功能需求) ★已同步
  ├── 代码实况 (**43,569 行**)        (实际实现参考)
  ├── common/schemas.py           (事件/数据结构定义)
  ├── data_engine/models.py       (Pydantic 数据模型)
  ├── event_hub/data_engine_schema.py (DDL Schema)
  ├── event_hub/routers/*         (API 端点实现)
  ├── deploy/*/docker-compose.yml (部署配置)
  └── edge/*/                     (边缘推理实现)
```

### E. 接口收敛状态矩阵（P1-B 新增）

> **新增于 v1.3 (2026-08-06)**：基于权威设计收敛审查，建立接口/端口/协议的统一基线。
>
> **问题背景**：代码演进过程中存在多套API前缀、端口分配未文档化、新旧协议并存等问题。
> **⚠️ v1.5 修正（评审反馈）**：本矩阵定义为「目标与现状差距表」，非"已完成清单"。状态列反映**已验证能力**，非目标状态。L3需真实门店7天闭环证据。

#### E.1 端口分配统一视图（目标 vs 现状差距表）

| 服务 | 端口 | 协议 | 部署位置 | 用途 | 目标契约 | 当前实现 | 验证状态 | 迁移期限 |
|:-----|:----:|:----:|----------|------|----------|----------|----------|----------|
| **Edge UI (FastAPI)** | **9080** | HTTP+WS | Jetson 边缘盒 | 门店配置管理界面 (13 API模块) | 15 API模块 + 认证 | 13 API模块(基础版) | 🔧 L2 Demo可用 | D1冲刺 |
| **Demo Web UI** | 8080 | HTTP | Jetson 边缘盒 | 展会演示页面 (5场景) | 7场景完整演示 | 5场景基本可用 | 🔧 L2 Demo | D4彩排前 |
| **Edge Agent API** | 8088 | HTTP | Jetson 边缘盒 | 内部推理引擎API | NL入口+订阅+超时 | 📋 规划中 | 📋 L1 设计中 | Phase2 |
| **Platform Dashboard** | 8098 | HTTP+WS | 腾讯云 43.139.143.12 | 平台端总管Dashboard | 多店KPI对比+预警 | 单店基础看板 | 🔧 L2 单店可用 | D3集成测试 |
| **Event Hub (内部)** | -- | 进程内调用 | Jetson | 事件总线 | Hub主写+统一契约 | Edge直写(旧模式) | ⚠️ 待改造 | P0阻塞 |
| **PostgreSQL** | 5432 | TCP | 云端 + Jetson本地 | 主数据库 | 双写+容灾切换 | PG双写(无容灾) | 🔧 L2 基础双写 | Phase2 |

#### E.2 API 前缀收敛规则

| 前缀模式 | 状态 | 使用范围 | 迁移计划 |
|----------|:----:|----------|----------|
| `/api/v1/*` | ✅ **推荐** | 所有新开发端点 | 当前标准 |
| `/v1/*` | ⚠️ 已废弃 | 部分早期端点 | v1.4 统一为 /api/v1 |
| 无前缀 `/*` | ❌ 已废弃 | Demo/测试端点 | 不再使用 |

**收敛示例**：
```
旧版（已废弃）：
  POST /ingest
  GET /devices

新版（当前标准）：
  POST /api/v1/events/ingest
  GET /api/v1/devices
```

#### E.3 核心端点收敛清单（Top 20 高频调用）

> **状态列含义**：✅=代码已实现 | 🔧=Demo/Mock | 📋=设计中等。**注意：✅ 不代表生产验证通过，仅表示端点可访问。**

| # | 方法 | 路径（收敛后） | 所属模块 | 认证方式 | 最后更新 | 状态 |
|:-:|:----:|----------------|----------|:--------:|:--------:|:----:|
| 1 | POST | `/api/v1/events/ingest` | EventHub | API Key | 2026-08-05 | ✅ 代码完成 |
| 2 | GET | `/api/v1/devices` | devices | API Key | 2026-08-02 | ✅ 代码完成 |
| 3 | POST | `/api/v1/devices/register` | devices | API Key | 2026-08-02 | ✅ 代码完成 |
| 4 | POST | `/api/v1/kitchen/waste` | kitchen | JWT | 2026-08-05 | ✅ 代码完成 |
| 5 | GET | `/api/v1/kitchen/waste/stats` | kitchen | JWT | 2026-08-05 | ✅ 代码完成 |
| 6 | POST | `/api/v1/receiving/submit` | receiving | JWT | 2026-08-04 | ✅ 代码完成 |
| 7 | GET | `/api/v1/receiving/list` | receiving | JWT | 2026-08-04 | ✅ 代码完成 |
| 8 | POST | `/api/v1/supplychain/purchase-order` | supply_chain | JWT+Gateway | 2026-08-02 | ✅ 含审批流 |
| 9 | GET | `/api/v1/supplychain/products` | supply_chain | JWT | 2026-08-03 | ✅ 代码完成 |
| 10 | POST | `/api/v1/assistant/chat` | assistant | JWT | 2026-08-04 | 🔧 NL入口待完善 |
| 11 | GET | `/api/v1/assistant/tasks` | assistant | JWT | 2026-08-04 | 🔧 NL入口待完善 |
| 12 | GET | `/api/v1/inventory/snapshot` | inventory | JWT | 2026-08-03 | ✅ 代码完成 |
| 13 | POST | `/api/v1/cameras/snapshot` | camera | Digest Auth | 2026-08-05 | ✅ HTTP抓拍可用 |
| 14 | GET | `/api/v1/system/health` | system | PIN→JWT过渡 | 2026-08-06 | ✅ 代码完成 |
| 15 | GET | `/api/v1/config/all` | config | JWT | 2026-08-05 | ✅ 代码完成 |
| 16 | POST | `/api/v1/gateway/audit-log` | gateway | Internal | 2026-08-02 | ✅ 审计日志正常 |
| 17 | GET | `/api/v1/alerts/list` | alerts | JWT | 2026-07-30 | 🔧 基础版 |
| 18 | POST | `/api/v1/vlm/review` | vlm | JWT | 2026-07-28 | 🔧 Mock模式 |
| 19 | GET | `/api/v1/daily-report/today` | daily_report | JWT | 2026-07-29 | 🔧 基础版 |
| 20 | WebSocket | `/ws/notifications` | notifications | JWT | 2026-08-04 | ✅ 连接可用 |

#### E.4 数据库连接串收敛

| 环境 | 连接串格式 | 主机 | 数据库 | 用途 |
|------|-----------|------|--------|------|
| **云端生产** | `postgresql://...@43.139.143.12:5432/hotpot_platform` | 腾讯云PG | hotpot_platform | 主存储（Hub主写） |
| **Jetson本地** | `postgresql://root@localhost:5432/hotpot_local` | 127.0.0.1 | hotpot_local | 缓存/离线降级 |
| **CI测试** | `postgresql://test@localhost:5432/hotpot_test` | localhost | hotpot_test | 单元/集成测试 |

#### E.5 待收敛项（Technical Debt）

| ID | 问题 | 影响 | 建议 | 优先级 |
|:--:|------|------|------|:------:|
| TD-1 | 部分路由仍用旧前缀（无/api/v1） | API风格不统一 | 批量重命名路由装饰器 | P1 |
| TD-2 | Edge Agent API(8088)未从UI拆分 | 耦合度高，无法独立扩展 | 抽取独立FastAPI app | P2 |
| TD-3 | VLM端点仍为Mock实现 | 无法真实验收S02/I02 | 接入多模态模型 | P0（阻塞L3） |
| TD-4 | 认证方式混用(PIN/JWT/API Key) | 安全策略不一致 | 全量迁移至JWT | P1（进行中） |
| TD-5 | Swagger/OpenAPI文档未自动生成 | 接口对接成本高 | 启用FastAPI自带docs | P2 |

#### E.6 API 端点迁移表（旧 → 新 · 唯一迁移依据）

> **用途**：正文§8映射表中标注的端点为**当前代码实现状态**（可能使用旧前缀）。本表是**唯一**的旧→新端点映射依据。
> **规则**：所有新开发必须直接使用目标端点。旧端点标记 `@deprecated`，保留兼容层至废弃日期。

| # | 旧端点（当前实现） | 目标端点（收敛标准） | 所属功能ID | 迁移版本 | 废弃日期 |
|:-:|---------------------|---------------------|:----------:|:--------:|:--------:|
| 1 | `POST /v1/suppliers` | `POST /api/v1/suppliers` | S01 | v1.6 | 2026-Q4 |
| 2 | `POST /v1/purchase-orders` | `POST /api/v1/purchase-orders` | S03 | v1.6 | 2026-Q4 |
| 3 | `POST /v1/suppliers/{id}/collab` | `POST /api/v1/suppliers/{id}/collab` | S04 | v1.6 | 2026-Q4 |
| 4 | `POST /v1/assistant/chat` | `POST /api/v1/assistant/chat` | A01-A03 | v1.6 | 2026-Q4 |
| 5 | `PATCH /v1/tasks/{id}` | `PATCH /api/v1/tasks/{id}` | A05 | v1.6 | 2026-Q4 |
| 6 | `POST /v1/sop/check` | `POST /api/v1/sop/check` | SC02 | v1.6 | 2026-Q4 |
| 7 | `GET/PATCH /v1/sop/violations` | `GET/PATCH /api/v1/sop/violations` | SC03 | v1.6 | 2026-Q4 |
| 8 | `CRUD /v1/sop/templates` | `CRUD /api/v1/sop/templates` | SC01 | v1.6 | 2026-Q4 |
| 9 | `POST /v1/knowledge/query` | `POST /api/v1/knowledge/query` | KT01-KT02 | v1.6 | 2026-Q4 |
| 10 | `POST /v1/warehouse/rfid/track` | `POST /api/v1/warehouse/rfid/track` | WH01 | v1.6 | 2026-Q4 |
| 11 | `GET /v1/warehouse/fevo/check` | `GET /api/v1/warehouse/fevo/check` | WH03 | v1.6 | 2026-Q4 |
| 12 | `GET /v1/warehouse/expiry/list` | `GET /api/v1/warehouse/expiry/list` | WH05 | v1.6 | 2026-Q4 |
| 13 | `POST /v1/members/visits` | `POST /api/v1/members/visits` | FH01 | v1.6 | 2026-Q4 |
| 14 | `GET /v1/members/{id}/profile` | `GET /api/v1/members/{id}/profile` | FH04 | v1.6 | 2026-Q4 |
| 15 | `GET /v1/national/dashboard` | `GET /api/v1/national/dashboard` | H02 | v1.6 | 2026-Q4 |
| 16 | `GET /v1/national/compare` | `GET /api/v1/national/compare` | H03 | v1.6 | 2026-Q4 |
| 17 | `POST /v1/national/inspection` | `POST /api/v1/national/inspection` | H06 | v1.6 | 2026-Q4 |
| 18 | `GET /v1/national/reports` | `GET /api/v1/national/reports` | H07 | v1.6 | 2026-Q4 |
| 19 | `POST /v1/iot/readings` | `POST /api/v1/iot/readings` | K18-K19 | v1.6 | 2026-Q4 |

> **注意**：已使用 `/api/v1/` 前缀的端点（如 N19/N22 的修正定义、N25 废料趋势等）无需迁移。

### D. 修订历史

| 版本 | 日期 | 作者 | 变更说明 |
|:----:|:----:|:----:|----------|
| v1.6 | 2026-08-06 | 系统架构师 | **第四轮评审修正(P0/P1/P2)**：①N19/N21/N22再次确认修正 ②§0文档关系v2.0→v2.1/v5.3i→v5.3n ③映射表标题标注PRD为唯一源 ④附录树版本同步 ⑤新增E.6 API端点迁移表(19项) ⑥版本号全量统一v1.5→v1.6 |
| v1.5 | 2026-08-06 | 系统架构师 | **第二轮评审修正**：①E.1端口表改为四列差距表 ②E.3端点状态添加免责声明 ③消除"生产就绪"表述 |
| v1.4 | 2026-08-06 | 系统架构师 | **P0-2修正**：N19/N21/N22功能定义对齐PRD + 上游引用更新至v5.3m |
| v1.3 | 2026-08-06 | 系统架构师 | **P1-B接口收敛**：①新增附录E端口分配统一视图(6服务) ②API前缀收敛规则(/api/v1标准) ③Top20核心端点收敛清单 ④DB连接串收敛(3环境) ⑤Technical Debt追踪(TD-1~TD-5) |
| v1.2 | 2026-08-06 | 系统架构师 | **P0-3/P0-4升级**：代码基线修正(43,569行) + 椒江5摄像机资产模型 + API前缀/ports标记legacy |
| v1.1 | 2026-07-30 | 系统架构师 | **重大升级**：①新增§1.6 SOP合规引擎(3类) ②新增§1.7知识库检索(混合RRF) ③新增§1.8仓库IoT(RFID/FEFO/温湿度/预警) ④新增§1.9会员营销(识别/积分/营销) ⑤DB表10→19张(+9表) ⑥§8映射矩阵Top20→完整129项功能 ⑦ER图+数据生命周期同步更新 |
| v1.0 | 2026-07-29 | 系统架构师 | 初版，基于代码实况全面梳理 |
