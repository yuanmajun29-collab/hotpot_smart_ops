# 火瞳 · 第三轮对齐硬验收清单报告 v2.0

> **轮次**：第三轮（2026-08-06 11:09 CST）
> **触发来源**：[`通用化修改方案` Ch10-12 + 甲方补充项](./火瞳_系统设置与架构通用化修改方案_v1.0.md)
> **Commit**：（本轮提交后填写）
> **范围**：feature/arch-design 全量文档 + 功能规格文档

---

## 验收总览

| # | 验收项 | 优先级 | 结果 | 说明 |
|---|--------|:------:|:----:|------|
| HC-1 | 链接完整性 | P0 | ✅ 通过 | 权威文档零外部断链 |
| HC-2 | 事实可追溯 | P0 | ✅ 通过 | §1.11.7已改为分层状态表，零虚高 |
| HC-3 | 配置式接入 | P1 | ⚠️ 有条件 | 文档层清零；代码层25+处待改 |
| HC-4 | 权限隔离 | P1 | ✅ 通过 | PRD/架构均有tenant_id/store_id校验定义 |
| HC-5 | 数据三态分离 | P1 | ✅ 通过 | source_status枚举+桩接口5值契约已固化 |
| HC-6 | Agent可配置 | P1 | ✅ 通过 | 四维最小字段清单已写入§1.11.5 |
| HC-7 | 接口一致性 | P2 | ✅ 通过 | 三份权威文档API定义一致 |
| HC-8 | 桩接口枚举固化 | P0补充 | ⚠️ 有条件 | 文档规范明确；代码侧存在mock/hybrid自造字段 |
| HC-9 | Agent四维字段 | P0补充 | ✅ 通过 | permissions/subscription/push_config/approval_config已定义 |
| HC-10 | 虚拟门店四步验收 | P0补充 | 📋 待执行 | 需代码侧配置化改造完成后验证 |

**通过率**：7/10 完全通过 + 3/10 有条件通过/待执行

---

## 逐项详情

### HC-1 链接完整性 ✅

**验证方法**：
```bash
grep -rn '\.docx)'\|'](\.\./\.\./\.\.' docs/01-核心权威/
```

**结果**：

| 文件 | 行号 | 命中内容 | 判定 |
|------|------|----------|------|
| 通用化方案_v1.0.md | 255 | `](../../../火瞳_...docx)` | ✅ 合理 — P0-A问题描述中引用原始错误（非活跃链接） |
| 硬验收报告_v1.0.md | 31,38 | grep命令示例 + 同上 | ✅ 合理 — 验收报告中复述问题 |
| PRD v5.3p | 71 | `.docx` | ✅ 合格 — 描述《最终方案》文件格式（非超链接） |

**结论**：零活跃断链。✅

---

### HC-2 事实可追溯 ✅

**验证方法**：逐项审查 PRD §1.11.7 分层状态表

**关键变更**（本轮）：
- 原6项全部 `[x]` → 改为**分层状态表**（文档层 vs 代码层）
- 明确标注：**文档层 6/6 完成，代码层 1/6 完成**
- 新增证据列：测试文件25+处硬编码、pipeline_config仍引用demo图片

**结论**：零虚高，所有"已完成"标注均有对应证据或明确标注"待代码实施"。✅

---

### HC-3 配置式接入 ⚠️ 有条件

**验证方法**：审计权威文档中的IP/端口/密码/ID

**文档层结果**：

| 检查项 | 上轮状态 | 本轮状态 |
|--------|----------|----------|
| 详细架构示例IP | 3处占位符 | ✅ 已改 `{HUB_URL}` / `{STREAM_URL}` |
| PRD §1.8 凭据 | 明文密码 | ✅ 已改 `{NVR_PASSWORD}` env |
| NVR证据链接路径 | `../../../` 多级 | ✅ 已修正为 `../` |

**代码层残留**（不阻塞文档验收，但记录在案）：

| 文件 | 硬编码数 | 示例 |
|------|----------|------|
| tests/test_sop_assign_api.py | 7处 | `"store_id": "store_yuhuan"` |
| tests/test_loss_restock_push.py | 10处 | `store_yuhuan` |
| tests/test_rbac_api.py | 8处 | `store_yuhuan` |
| scripts/train_table_classifier.py | 1处 | `--store-id store_yuhuan` |
| edge/common/config/pipeline_config_jiaojiang.yml | 1处 | `demo/data/real_hotpot_waste.jpg` |

**结论**：文档层 ✅ 通过；代码层 ⚠️ 记录为 **配置化改造Sprint 待办项**。

---

### HC-4 权限隔离 ✅

**验证内容**：
- PRD §1.11.2 默认门店规范：明确"默认值不得参与权限判断"
- 总体架构 v2.1 API鉴权层：100% 携带 tenant_id/store_id
- 详细架构 v1.6 RBAC矩阵：角色→数据范围映射完整

**结论**：✅ 通过。

---

### HC-5 数据三态分离 ✅

**验证内容**：
- PRD §1.11.6 Demo/Mock/桩接口隔离规范：5级环境分类表
- **桩接口返回码约定**（本轮强化）：
  - 5个固定枚举：`not_connected` / `disabled` / `simulated` / `pending_integration` / `error`
  - 强制规则："各模块不得自造字段名"
  - 违规判定："生产环境出现 simulated 视为P0异常"
- source_status 在事件/任务/KPI 中沿用此枚举（+ real）

**结论**：✅ 通过。

---

### HC-6 Agent 可配置 ✅

**验证内容**（本轮新增）：

| 维度 | 字段名 | 子字段 | 状态 |
|------|--------|--------|:----:|
| 权限 | `permissions` | can_query, can_advise, can_push, can_create_todo, can_approve | ✅ 新增 |
| 订阅 | `subscription` | event_types[], kpi_metrics[], time_window, thresholds | ✅ 新增 |
| 推送 | `push_config` | channels[], priority_rules, escalation_min, ack_timeout_sec | ✅ 新增 |
| 审批 | `approval_config` | require_approval_for[], auto_approve_threshold, escalate_to_role | ✅ 新增 |

四维最小字段清单已写入 PRD §1.11.5，并附强制说明："新增岗位Agent必须先完成此四维配置方可上线"。

**结论**：✅ 通过。

---

### HC-7 接口一致性 ✅

**验证方法**：对比三份权威文档的API定义

| 检查项 | PRD v5.3p | 总体架构 v2.1 | 详细架构 v1.6 | 一致性 |
|--------|-----------|--------------|-------------|:------:|
| 事件前缀 | `/api/v1/events` | `/api/v1/events` | `/api/v1/events` | ✅ |
| 事件字段数 | 16项全部必填 | 16项 | 16项 | ✅ |
| Agent配置维度 | 5维+4维清单 | 5维 | 5维 | ✅ |
| 桩接口枚举 | 5固定值 | 5固定值 | 5固定值 | ✅ |

**结论**：✅ 通过。

---

### HC-8 桩接口枚举固化 ⚠️ 有条件

**文档规范**：✅ PRD §1.11.6 已写入5值固定枚举 + 强制规则

**代码侧现状**（待改造）：

| 文件 | 自造字段 | 问题 |
|------|----------|------|
| demo/r7_demo_annotator.py | `mock`, `hybrid` | 不在5值枚举中 |
| demo/expo_demo.py | `fallback_simulated` | 应归入 `simulated` 或 `pending_integration` |

**结论**：⚠️ 文档规范通过；代码侧记录为 **配置化改造Sprint 待办**（统一替换为标准枚举）。

---

### HC-9 Agent 四维最小字段 ✅

PRD §1.11.5 已包含完整的四维最小字段清单表格，含字段名、类型、说明。详见 HC-6。✅

---

### HC-10 虚拟门店四步验收 📋 待执行

**前置条件**：HC-3 代码层改造完成（25+处硬编码清除）后执行

**验收步骤**：

| 步骤 | 操作 | 预期结果 | 状态 |
|------|------|----------|:----:|
| 1 | 配置录入 `store_virtual`（组织/设备/区域/规则/权限） | 配置保存成功，无报错 | 📋 待执行 |
| 2 | 用 `store_virtual` 账号登录 | 鉴权通过，Token携带正确 tenant_id/store_id | 📋 待执行 |
| 3 | 打开看板页面 | 显示 `store_virtual` 的数据（非椒江店数据） | 📋 待执行 |
| 4 | 触发告警 + Agent推送 | 告警事件携带 `store_virtual`；Agent推送可达 | 📋 待执行 |

**通过标准**：全流程零代码修改，仅通过配置操作完成。

---

## 本轮修复清单

| # | 修复项 | 文件 | 变更类型 |
|---|--------|------|----------|
| 1 | 事件字段数统一口径：**9通用必填 + 3感知来源 + 4视觉扩展**，共计16字段 | PRD §1.11.4 | 去歧义 |
| 2 | 事件字段表去掉"分类"列 | PRD §1.11.4 | 表结构优化 |
| 3 | 新增 **Agent 四维最小字段清单** | PRD §1.11.5 | 新增20行 |
| 4 | 桩接口枚举 4值 → **5值** + 强制规则 | PRD §1.11.6 | 强化 |
| 5 | §1.11.7 `[x]` 全部 → **分层状态表** | PRD §1.11.7 | 去伪存真 |
| 6 | 功能规格 28处 `store_yuhuan` → `store_demo` | 6个spec/test_cases文件 | 去硬编码 |
| 7 | 通用化方案追加 HC-8/9/10 + 执行记录表 | 通用化方案_v1.0.md | 补充3项 |

---

## 遗留项（转入配置化改造Sprint）

| # | 遗留项 | 涉及文件 | 预估工作量 |
|---|--------|----------|-----------|
| L-1 | 测试文件 25+处 store_yuhuan → 配置注入 | tests/test_*.py (3文件) | 0.5h |
| L-2 | 脚本硬编码门店ID | scripts/train_table_classifier.py | 0.25h |
| L-3 | pipeline_config demo图片引用 | edge/common/config/pipeline_config_jiaojiang.yml | 0.25h |
| L-4 | 代码侧桩接口自造字段统一 | demo/r7_demo_annotator.py, expo_demo.py | 0.5h |
| L-5 | 虚拟门店四步端到端验收 | 全流程 | 1h |
