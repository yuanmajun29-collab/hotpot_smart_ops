# K-002 白盒审查报告

> 审查日期: 2026-07-16 | 审查人: Hermes (小马) + 功能测试 | 参考: spec-K002, test_cases-K002

## 一、变更文件清单

| 文件 | 任务要求 | 实际状态 | 说明 |
|------|:---:|:---:|------|
| domain/waste_timeseries.py | 新建 | ✅ | 纯函数：聚合/对比/告警 |
| db.py | 修改 | ✅ | 添加2新表DDL + 5新方法 |
| routers/kitchen.py | 修改 | ✅ | 追加4新端点，保留旧端点 |
| dashboard/kitchen-count.html | 修改 | ✅ | 30天SVG折线图+环比卡片+告警指示 |

## 二、逐文件审查

### 2.1 domain/waste_timeseries.py

| # | 维度 | 状态 | 说明 |
|---|------|:---:|------|
| 1 | Spec符合性 | ✅ | 函数签名/返回值/对比规则全部对齐spec |
| 2 | 边界异常 | ✅ | 空events、零基线、不足14天数据均正确处理 |
| 3 | 安全 | ✅ | 纯函数，无IO |
| 4 | 性能 | ✅ | O(n) 线性扫描，无无界增长 |

✅ 正确实现:
- aggregate: 支持total_waste_count直接取值 + items累加两种模式
- comparison: 日环比/周环比/7d均值/30d均值计算正确
- alert: zero基线不触发告警
- format: 中文消息格式化

🟢 P2-1: top_skus限制10条，大品类店可能不够（可调参数）

### 2.2 db.py

| # | 维度 | 状态 | 说明 |
|---|------|:---:|------|
| 1 | Spec符合性 | ✅ | DDL + 5个方法全部实现 |
| 2 | 边界异常 | ✅ | 空表返回全0填充，JSON解析异常catch |
| 3 | 安全 | ✅ | SQL参数化查询，无注入风险 |
| 4 | 性能 | ✅ | _lock保护，聚集索引idx_wts_store_date |
| 5 | 现有代码冲突 | ✅ | 不修改现有方法，纯追加 |

✅ 正确实现:
- UNIQUE(store_id, date) + INSERT ON CONFLICT DO UPDATE (UPSERT)
- UNIQUE(store_id, date, alert_type) + INSERT OR IGNORE (幂等告警)
- query_waste_trend 缺失日期0填充
- check_and_create_waste_alert 分两次lock（读→写）避免长时间持锁

🟡 P1-1: check_and_create_waste_alert 两次 acquire lock（读+写），存在微小竞态窗口——同日同店并发check可能重复写入。但UNIQUE约束兜底，INSERT OR IGNORE保证数据正确性，不会出错只是可能浪费一次尝试。

### 2.3 routers/kitchen.py

| # | 维度 | 状态 | 说明 |
|---|------|:---:|------|
| 1 | Spec符合性 | ✅ | 4个端点全部实现 |
| 2 | 边界异常 | ✅ | days参数验证ge=1,le=90，ack 404处理 |
| 3 | 安全 | ✅ | 复用现有enforce_store_write认证 |
| 4 | 现有代码冲突 | ✅ | 保留原有waste/stats端点不变 |

✅ 正确实现:
- AlertCheckBody Pydantic模型
- 可选body的POST端点（无body时从DB查）
- Ack端点404覆盖

### 2.4 dashboard/kitchen-count.html

| # | 维度 | 状态 | 说明 |
|---|------|:---:|------|
| 1 | Spec符合性 | ✅ | 30天SVG折线图 + 环比Hero卡片 + 告警指示 |
| 2 | UI测试 | ✅ | 页面可访问，数据正确渲染 |
| 3 | 性能 | ✅ | 60s刷新间隔，SVG轻量渲染 |

✅ 正确实现:
- 5 Hero卡片：今日总数/7d均值/日环比/周环比/30d均值
- SVG折线图：带区域渐变填充、7d/30d虚线参考线、数据点标记
- 告警bar：未确认告警显示红色banner + 一键确认
- 品类分布 + 事件日志保留

## 三、安全检查

| 检查项 | 结果 |
|--------|:---:|
| SQL注入 | ✅ 全部参数化查询 |
| 未授权访问 | ✅ enforce_store_write |
| 明文密钥 | ✅ 无硬编码密钥 |
| 认证链路 | ✅ 复用AuthContext |

## 四、性能分析

| 检查项 | 结果 |
|--------|:---:|
| 死循环 | ✅ 无 |
| 内存泄漏 | ✅ 无无界列表 |
| 连接池复用 | ✅ SQLite单连接+锁 |
| 查询延迟 | ✅ <10ms (30行聚合) |

## 五、问题汇总

| 严重度 | 数量 | ID列表 |
|:---:|:---:|------|
| 🔴 P0 | 0 | — |
| 🟡 P1 | 1 | P1-1 (并发微小竞态，UNIQUE兜底) |
| 🟢 P2 | 1 | P2-1 (top_skus可调参数化) |

## 六、功能测试结果

| 测试 | 结果 |
|------|:---:|
| API GET /trend (30天空表) | ✅ 200, 全0填充 |
| API GET /trend (有数据) | ✅ 正确返回趋势+对比 |
| API POST /alerts/check (触发) | ✅ 200>100×1.5, alert_triggered=true |
| API POST /alerts/check (不触发) | ✅ 100 vs 0基线, alert_triggered=false |
| API POST /alerts/check (幂等) | ✅ 重复POST不创建重复告警 |
| API GET /alerts | ✅ 正确列出告警 |
| API POST /alerts/{id}/ack | ✅ 正确确认，404覆盖 |
| 现有 /waste/stats | ✅ 不受影响 |

## 七、准出结论

✅ **准出通过** — 无P0问题，P1有UNIQUE兜底无实际风险，所有API功能测试通过。
