# 火瞳 · 通用化硬验收清单报告 v1.0

> **版本**：v1.0 | **日期**：2026-08-06 | **状态**：📋 第二轮对齐验收
> **依据**：[`通用化修改方案` Chapter 12](./火瞳_系统设置与架构通用化修改方案_v1.0.md)
> **验收范围**：`docs/01-核心权威/` 全部 7 份 Markdown 文档

---

## 验收总览

| # | 验收维度 | 结果 | 发现问题 | 已修复 |
|---|----------|:----:|----------|:------:|
| HC-1 | 链接完整性 | ⚠️ 有条件通过 | N04证据链接路径偏移(../../../→../../) | ✅ |
| HC-2 | 事实可追溯 | ✅ 通过 | 零虚高（前四轮整改已去伪） | — |
| HC-3 | 配置式接入 | ⚠️ 有条件通过 | 详细架构3处示例IP硬编码 | ✅ |
| HC-4 | 权限隔离 | ✅ 通过 | 全量API/事件/Agent携带上下文 | — |
| HC-5 | 数据三态分离 | ✅ 通过 | source_status在schema/环境表/验收标准中全覆盖 | — |
| HC-6 | Agent可配置 | ✅ 通过 | 五维配置模型在PRD§1.11.5+通用化方案Ch6双写 | — |
| HC-7 | 接口一致性 | ✅ 通过 | /api/v1前缀在三份架构文档中统一 | — |

**总体判定**：**⚠️ 有条件通过 —— 2 项非阻塞性问题已当场修复，零 P0 阻塞项。**

---

## 逐项详情

### HC-1 链接完整性

**验证方法**：
```bash
grep -rn '\.docx)\|]\(\.\./\.\./\.\.' docs/01-核心权威/
```

**结果**：

| 命中位置 | 内容 | 判定 |
|----------|------|------|
| 通用化方案.md:255 | `](../../../火瞳_...docx)` | ✅ 合理 — 在P0-A问题描述中引用原始错误（非活跃链接） |
| PRD.md:71 | `\| .docx \|` | ✅ 合格 — 描述《最终方案》文件格式（非超链接） |
| PRD.md:454 | `](../../../yuhuan_waste_report.md)` | ❌ **路径错误** — 多一级 `..` |

**修复动作**：`../../../yuhuan_waste_report.md` → `../yuhuan_waste_report.md`（✅ 已修复）

**修复后验证**：
```bash
# 目标文件实际位置
docs/yuhuan_waste_report.md  ← 从 docs/01-核心权威/ 出发应为 ../
```

---

### HC-2 事实可追溯

**验证方法**：逐项审查 PRD §3 功能清单"当前状态"列 + §1.9 成熟度矩阵，交叉对照代码库 Commit 历史。

**结果**：

| 检查项 | 数据 |
|--------|------|
| 声称 L3 的功能数 | 1 项（N04） |
| L3 证据完整性 | ✅ 有完整证据链（玉环店7天+7/7标准） |
| 声称 L2 的功能数 | ~22 项 |
| L2 虚高（实为L1） | **0 项** |
| 历史降级记录 | ✅ PRD 明确记载"原3项L3经证据审查后全部降级" |
| 对照表文档 | ✅ 已生成 [`目标vs实现状态对照表_v1.0`](./火瞳_目标vs实现状态对照表_v1.0.md) |

**结论**：**前四轮 P0 整改已彻底完成去伪存真工作。本次审计未发现新的虚高问题。**

---

### HC-3 配置式接入（零硬编码）

**验证方法**：
```bash
grep -rn '192\.168\.\|172\.16\.\|hy898989\|123456' docs/01-核心权威/*.md
```

**结果**：

| 命中位置 | 内容 | 判定 |
|----------|------|------|
| PRD §1.8 椒江设备表 (2处) | `192.168.5.102`, `192.168.6.21` | ✅ 合规 — 位于§1.8样板店实例章节（通用化规则允许） |
| 总体架构 VLAN规划 (6处) | `192.168.10.0/24` 等 | ✅ 合规 — 网络拓扑目标设计（非具体设备地址） |
| 详细架构代码示例 (3处) | `192.168.2.85:8088`, `192.168.2.10:554` | ❌ **应使用占位符** |

**修复动作**：
- `"http://192.168.2.85:8088"` → `"{HUB_URL}"` （✅ 已修复）
- `"rtsp://192.168.2.10:554/stream1"` → `"rtsp://{NVR_IP}:554/{STREAM_PATH}"` （✅ 已修复）
- hub_url 参数注释中的示例 → `"{HUB_URL}"` （✅ 已修复）

**密码/凭据检查**：✅ 零明文密码。所有凭据已替换为 `{NVR_PASSWORD}` / `{EDGE_TOKEN}` 环境变量引用。

---

### HC-4 权限隔离（全量上下文携带）

**验证方法**：检查 API/事件/Task/Agent 入参定义是否 100% 携带 `tenant_id` / `store_id`。

**结果**：

| 层级 | tenant_id/store_id 携带情况 | 服务端校验描述 |
|------|---------------------------|---------------|
| 事件 schema（§1.11.4） | ✅ 16 字段含 tenant_id + store_id | EventHub 写入时校验 |
| Agent 配置（§1.11.5） | ✅ org_scope 含 store_ids 列表 | Gateway 中间件校验 |
| API 设计（详细架构） | ✅ 全部 /api/v1/* 端点 | 中间件统一注入 |
| 审计日志 | ✅ agent_gateway.audit_log | 含完整上下文 |

**参考文档**：PRD §1.11.2 默认门店规范 — "所有 API、事件、任务和 Agent 请求**必须显式携带**并由服务端校验"

**结论**：**通过**。权限隔离设计已在 PRD §1.11 + Agent Gateway 架构中完整定义。

---

### HC-5 数据三态分离（source_status 覆盖）

**验证方法**：检查 real/stub/simulated 三态在接口、存储、看板、Agent 四层的区分逻辑。

**结果**：

| 层级 | source_status 使用 | 覆盖状态 |
|------|-------------------|----------|
| 接口层（事件 schema） | ✅ 第 16 字段必填 | 已覆盖 |
| 存储层（DB 表） | ✅ events 表 source_status 列 | 已覆盖 |
| 看板层（Dashboard） | ✅ 三态数据过滤逻辑描述 | 已覆盖 |
| Agent 层（审计日志） | ✅ audit_log 含 source_status | 已覆盖 |
| 环境隔离（§1.11.6） | ✅ 5 级环境 × Mock允许矩阵 | 已覆盖 |

**结论**：**通过**。

---

### HC-6 Agent 可配置化（五维驱动）

**验证方法**：检查各 Agent 定义是否通过 5 维配置驱动，无单店硬编码。

**结果**：

| 配置维度 | PRD §1.11.5 | 通用化方案 Ch6 | Agent Gateway 代码 |
|----------|-------------|----------------|-------------------|
| 角色 role | ✅ 定义 | ✅ 定义 | ✅ action_types.py |
| 组织范围 org_scope | ✅ 定义 | ✅ 示例 | ✅ PermissionMatrix |
| 数据范围 data_scope | ✅ 定义 | ✅ 示例 | ✅ 查询过滤器 |
| 订阅策略 subscription | ✅ 引用§1.10.2 | ✅ 引用 | ✅ 事件订阅器 |
| 动作策略 action_policy | ✅ 定义 | ✅ 红线说明 | ✅ Gateway 审批 |

**红线验证**：✅ "自动下单/扣库存 → ❌需审批" 在全部 3 处一致声明。

**结论**：**通过**。

---

### HC-7 接口一致性（三文档对比）

**验证方法**：对比 PRD / 总体架构 v2.1 / 详细架构 v1.6 中同一 API 的路径、方法、入参、出参。

**抽样检查结果**（Top 10 核心端点）：

| API 端点 | PRD | 总体架构 | 详细架构 | 一致性 |
|----------|-----|----------|----------|:------:|
| POST /api/v1/events | ✅ | ✅ Hub主写模式 | ✅ EventHub router | ✅ |
| POST /api/v1/purchase-orders | ✅ S03 | ✅ | ✅ 含审批流 | ✅ |
| POST /api/v1/assistant/chat | ✅ A01-A03 | ✅ NL入口 | ✅ Assistant API | ✅ |
| GET /api/v1/dashboard | ✅ K06 | ✅ 单店看板 | ✅ Dashboard | ✅ |
| GET /api/v1/stores/{id}/inventory | ✅ N03 | ✅ | ✅ 库存快照 | ✅ |
| GET /api/v1/suppliers | ✅ S01 | ✅ | ✅ PG CRUD | ✅ |
| POST /api/v1/waste/events | ✅ K01 | ✅ 视觉事件 | ✅ YOLO router | ✅ |
| PATCH /api/v1/tasks/{id} | ✅ A05 | ✅ | ✅ Task 更新 | ✅ |
| GET /api/v1/tables/status | ✅ K04 | ✅ 桌态 | ✅ TableService | ✅ |
| POST /api/v1/sop/check | ✅ SC02 | ✅ | ✅ SOP 引擎 | ✅ |

**版本号一致性**：✅ 三文档统一引用 **PRD v5.3q / 总体架构 v2.1 / 详细架构 v1.6**（本轮 #483 统一）。

**结论**：**通过**。

---

## 遗留问题跟踪

| ID | 问题 | 严重度 | 状态 | 建议修复时限 |
|----|------|:------:|:----:|-------------|
| N/A | 全部发现的问题均已在本轮修复 | — | — | — |

---

## 签署

| 角色 | 确认 |
|------|------|
| 文档审计方 | 火瞳项目组（第二轮对齐） |
| 审计日期 | 2026-08-06 01:39 CST |
| 审计基线 | PRD v5.3q + 总体架构 v2.1 + 详细架构 v1.6 + 通用化方案 v1.0 |
| 工具链 | grep + find + 人工交叉审读 |
